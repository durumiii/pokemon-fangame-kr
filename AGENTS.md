# pokemon-fangame-kr — 게이트

포켓몬 팬게임 한국어 패치를 만드는 곳. **게임 하나가 폴더 하나**이고, 작업 규율과 문서는
그 폴더 안에 있다.

| 게임 | 폴더 | 게이트 |
|---|---|---|
| Pokémon Z Fangame | `pokemon-z/` | [pokemon-z/AGENTS.md](pokemon-z/AGENTS.md) |

**손대기 전에 그 폴더의 `AGENTS.md`를 읽는다.** 이 문서는 어느 폴더로 갈지만 알려 준다 —
번역 정본·지침·판정 대장·티켓은 전부 게임 폴더 안이다.

작업은 그 폴더에서 한다. 셸을 열면 먼저 `cd pokemon-z`(뿌리에서 시작할 때) — 도구들이
저장소 뿌리가 아니라 게임 폴더를 기준으로 경로를 잡는다.

⚠ **다른 세션이 이 체크아웃을 쓰고 있으면 워크트리를 판다.** 남의 브랜치 포인터를 옮기지
마라 — 2026-08-03에 세 세션이 한 체크아웃을 공유하다 커밋 셋이 브랜치에서 이탈했고,
회수하느라 같은 작업이 두 번 커밋됐다.

저장소 뿌리의 `docs/`에는 게임을 안 가리는 것만 둔다(용어 메모 등).
