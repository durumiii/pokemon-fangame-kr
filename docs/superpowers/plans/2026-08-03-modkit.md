# modkit 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** fanlib의 모드 코어를 독립 프로젝트 modkit으로 이사시켜 정본으로 삼고, 진단·선언 검사·CLI·SPEC·GUI exe까지 갖춘 Essentials 모드 표준 도구를 만든다.

**Architecture:** modkit은 의존 하나(rubymarshal)의 순수 파이썬 패키지 + pywebview GUI. fanlib은 `sys.modules` 치환 심으로 modkit을 소비한다. 스펙: `docs/superpowers/specs/2026-08-03-modkit-design.md` (pokemon-fangame-kr repo).

**Tech Stack:** Python 3.12+/uv, rubymarshal, pytest, pywebview(Phase B), PyInstaller(Phase B, Windows).

## Global Constraints

- modkit 저장소: `/home/durumii/workspace/claude-native/sketches/modkit` (새 git repo). fanlib: `/home/durumii/workspace/claude-native/sketches/fangame-library` (기존 repo).
- modkit 코어는 fanlib을 절대 임포트하지 않는다. 외부 의존은 rubymarshal 하나 (GUI 단계에서 pywebview 추가).
- 이사하는 6모듈(modstore·modassets·modfit·scripts·rubyread·rubywrite)의 기존 동작·주석·docstring은 보존한다 — 수정은 의존 절단 지점만.
- 파괴 동사 금지: 삭제 대신 격리(`_quarantine/`). 첫 쓰기 전 `.orig` 백업, 있으면 안 덮음. 제자리 수정 금지(옆에 쓰고 rename).
- CRLF 보존: 스크립트 파일은 `newline=""`로 읽고 쓴다.
- 커밋은 Conventional Commits, Co-Authored-By 없음. modkit 작업은 modkit repo에, fanlib 작업은 fangame-library repo에 커밋.
- 코드 주석·docstring은 기존 fanlib 문체(한국어 서술형)를 따른다.

---

## Phase A — 코어 이사·진단·선언·CLI

### Task 1: modkit 스캐폴드

**Files:**
- Create: `~/workspace/claude-native/sketches/modkit/pyproject.toml`
- Create: `~/workspace/claude-native/sketches/modkit/modkit/__init__.py`
- Create: `~/workspace/claude-native/sketches/modkit/AGENTS.md`
- Create: `~/workspace/claude-native/sketches/modkit/docs/design.md` (스펙 사본)
- Create: `~/workspace/claude-native/sketches/modkit/.gitignore`

**Interfaces:**
- Produces: 이후 모든 태스크의 작업 디렉토리. 테스트 실행은 `cd ~/workspace/claude-native/sketches/modkit && uv run pytest`.

- [ ] **Step 1: 디렉토리·git·pyproject 생성**

```bash
mkdir -p ~/workspace/claude-native/sketches/modkit/{modkit,tests,docs}
cd ~/workspace/claude-native/sketches/modkit && git init -q
cp ~/workspace/claude-native/pokemon-fangame-kr/docs/superpowers/specs/2026-08-03-modkit-design.md docs/design.md
touch modkit/__init__.py
printf '__pycache__/\n*.egg-info/\ndist/\nbuild/\n.venv/\n' > .gitignore
```

`pyproject.toml`:

```toml
[project]
name = "modkit"
version = "0.1.0"
description = "Essentials 팬게임 모드 표준 단위와 매니저"
requires-python = ">=3.12"
dependencies = ["rubymarshal"]

[dependency-groups]
dev = ["pytest"]
```

- [ ] **Step 2: AGENTS.md 씨앗 작성**

docs/design.md의 「배경과 목표」 절을 요약한 10줄 내외 + 「스펙은 docs/design.md, 계획은 pokemon-fangame-kr repo의 docs/superpowers/plans/2026-08-03-modkit.md」 포인터 + Global Constraints의 안전 규약 4줄을 담는다.

- [ ] **Step 3: pytest가 빈 상태로 도는지 확인**

Run: `cd ~/workspace/claude-native/sketches/modkit && uv run pytest`
Expected: `no tests ran` (exit 5여도 정상 — 수집 0개 확인이 목적)

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "chore: modkit 스캐폴드 — 설계 문서·pyproject·패키지 뼈대"
```

### Task 2: 코어 6모듈 이사 + 의존 절단 (gameinfo)

**Files:**
- Create: `modkit/rubyread.py`, `modkit/rubywrite.py`, `modkit/scripts.py`, `modkit/modassets.py`, `modkit/modfit.py`, `modkit/modstore.py` (fanlib 동명 파일 사본)
- Create: `modkit/gameinfo.py`
- Test: `tests/test_import_standalone.py`

**Interfaces:**
- Produces: `modkit.modstore`의 공개 API는 fanlib 시절과 동일 — `installed(game_dir)`, `apply(store, name, game_dir)`, `remove(name, game_dir, store=None)`, `harvest(game_dir, names, store=DEFAULT_STORE, game=None, at=None, version=None)`, `read_mod(store, name)`, `shelf(store, game=None)`, `rename(...)`, `describe(...)`, `same_core(a, b)`. 예외: `NoBundle`, `ModMissing`, `BaseChanged`, `WrongGame`, `NameTaken`.
- Produces: `modkit.gameinfo.read_title(game_dir) -> str`, `modkit.gameinfo.now() -> str`.
- Produces: `DEFAULT_STORE = Path(os.environ.get("MODKIT_STORE", "mods"))`.

- [ ] **Step 1: 실패하는 테스트 작성** — fanlib 없이 임포트되고 합성 게임에서 harvest→apply→remove 왕복이 되는지

`tests/test_import_standalone.py`:

```python
"""modkit이 fanlib 없이 홀로 서는지 — 임포트와 왕복."""
import sys
import zlib
from pathlib import Path

import pytest


def test_no_fanlib_import():
    for name in ("modstore", "modassets", "modfit", "scripts",
                 "rubyread", "rubywrite", "gameinfo"):
        mod = __import__(f"modkit.{name}", fromlist=[name])
        src = Path(mod.__file__).read_text(encoding="utf-8")
        assert "fanlib" not in src, f"{name}이 아직 fanlib을 물고 있다"


def make_game(tmp_path, title="Test Game"):
    """묶음(PluginScripts.rxdata) 있는 합성 설치본."""
    from modkit import rubywrite
    game = tmp_path / "game"
    (game / "Data").mkdir(parents=True)
    (game / "Game.ini").write_text(f"[Game]\nTitle={title}\n", encoding="utf-8")
    entries = [["Base Mod", {}, [["001_Base.rb",
                zlib.compress(b"class Foo\r\n  def bar\r\n  end\r\nend\r\n")]]]]
    (game / "Data" / "PluginScripts.rxdata").write_bytes(rubywrite.dumps(entries))
    return game


def test_roundtrip_harvest_apply_remove(tmp_path):
    from modkit import modstore
    game = make_game(tmp_path)
    store = tmp_path / "store"

    got = modstore.harvest(game, ["Base Mod"], store=store)
    assert got[0].name == "Base Mod"
    assert got[0].game == "Test Game"          # gameinfo.read_title 경유
    assert (store / "Test Game" / "Base Mod" / "mod.json").is_file()

    before = (game / "Data" / "PluginScripts.rxdata").read_bytes()
    modstore.remove("Base Mod", game, store=store)
    assert modstore.installed(game) == []
    modstore.apply(store, "Base Mod", game)
    assert modstore.installed(game) == ["Base Mod"]
    after = (game / "Data" / "PluginScripts.rxdata").read_bytes()
    assert before == after                      # 빼기→얹기 왕복 바이트 동일


def test_gameinfo_title_fallback(tmp_path):
    from modkit import gameinfo
    assert gameinfo.read_title(tmp_path) == tmp_path.name  # Game.ini 없으면 폴더 이름
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_import_standalone.py -v`
Expected: FAIL — `ModuleNotFoundError: modkit.modstore` 등

- [ ] **Step 3: 6모듈 복사**

```bash
F=~/workspace/claude-native/sketches/fangame-library/fanlib
cp $F/{rubyread,rubywrite,scripts,modassets,modfit,modstore}.py modkit/
```

- [ ] **Step 4: gameinfo.py 작성**

`modkit/gameinfo.py` — fanlib entry.read_title(Game.ini의 Title, UTF-8→CP949 순서로 읽고 없으면 폴더 이름)과 launch.now()의 최소 이식:

```python
"""설치본의 신원 — 게임 제목과 시각.

fanlib entry.read_title의 이식본이다. 옛 RGSS 엔진은 Game.ini를 ANSI
(한국어 Windows에선 CP949)로 읽으므로 UTF-8이 먼저, 안 읽히면 CP949로 다시.
"""
import configparser
from datetime import datetime
from pathlib import Path


def read_title(game_dir: Path | str) -> str:
    """`Game.ini`의 Title을 읽고, 없거나 깨졌으면 폴더 이름을 쓴다."""
    game_dir = Path(game_dir)
    ini = game_dir / "Game.ini"
    if ini.is_file():
        for encoding in ("utf-8", "cp949"):
            parser = configparser.ConfigParser()
            try:
                parser.read(ini, encoding=encoding)
                title = parser.get("Game", "Title", fallback="").strip()
            except (configparser.Error, UnicodeDecodeError):
                continue
            if title:
                return title
    return game_dir.name


def now() -> str:
    return datetime.now().astimezone().isoformat(timespec="seconds")
```

- [ ] **Step 5: modstore.py 의존 절단** — 고칠 곳은 정확히 넷

1. `DEFAULT_STORE = Path("/mnt/d/GameVault/mods")` →

```python
import os
DEFAULT_STORE = Path(os.environ.get("MODKIT_STORE", "mods"))
```

2. `apply()` 안의 `from . import entry as game_entry` → `from . import gameinfo`, `game_entry.read_title(game_dir)` → `gameinfo.read_title(game_dir)`.

3. `harvest()` — 시그니처에 `version: str | None = None` 추가, 본문의 fanlib 삼중 임포트를 절단:

```python
# 기존:
#   from . import entry as game_entry, fingerprint, launch
#   belongs_to = game or game_entry.read_title(game_dir)
#   when = at or launch.now()
#   try:
#       version = fingerprint.of(game_dir).version_id
#   except Exception:
#       version = ""
# 교체:
    from . import gameinfo

    belongs_to = game or gameinfo.read_title(game_dir)
    when = at or gameinfo.now()
    version = version or ""   # 버전 지문은 부르는 쪽(fanlib 등)이 알면 준다
```

4. 파일 전체에서 그 외 `from . import` 대상이 modassets·rubyread·rubywrite·modfit·gameinfo뿐인지 grep으로 확인 (`grep -n "from . import" modkit/*.py`).

- [ ] **Step 6: 통과 확인**

Run: `uv run pytest tests/test_import_standalone.py -v`
Expected: PASS 3건

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat: fanlib 모드 코어 6모듈 이사 — gameinfo로 의존 절단, 왕복 테스트"
```

### Task 3: 주입형 계약 회귀 테스트

이사가 주입 경로(묶음 없는 옛 엔진)를 깨지 않았는지. 코드 수정 없이 테스트만 더한다 — 이후 태스크(선언 검사)가 apply를 고치므로 그 전에 그물을 친다.

**Files:**
- Test: `tests/test_inject.py`

**Interfaces:**
- Consumes: Task 2의 `modkit.modstore` API, `modkit.rubywrite.dumps`.
- Produces: `tests/test_inject.py::make_core_game(tmp_path)` — Scripts.rxdata만 있는 합성 설치본 헬퍼(이후 태스크 재사용).

- [ ] **Step 1: 테스트 작성**

```python
"""주입형 계약 — Scripts.rxdata의 MOD: 섹션 왕복."""
import json
import zlib
from pathlib import Path

import pytest


def make_core_game(tmp_path, sections=(("Main", b"# main\r\n"),)):
    """묶음 없는 옛 엔진 설치본 — Scripts.rxdata 코어 배열만 있다."""
    from modkit import rubywrite
    game = tmp_path / "oldgame"
    (game / "Data").mkdir(parents=True)
    (game / "Game.ini").write_text("[Game]\nTitle=Old Game\n", encoding="utf-8")
    entries = [[i + 1, title, zlib.compress(body)]
               for i, (title, body) in enumerate(sections)]
    (game / "Data" / "Scripts.rxdata").write_bytes(rubywrite.dumps(entries))
    return game


def put_mod(store, name, source=b"def patched\r\nend\r\n", game="Old Game", extra=None):
    """보관소에 주입형 모드 카드를 손으로 눕힌다."""
    folder = store / game / name
    folder.mkdir(parents=True)
    (folder / "001_Mod.rb").write_bytes(source)
    card = {"name": name, "game": game, "scripts":
            [{"file": "001_Mod.rb", "script_name": "001_Mod.rb"}]}
    card.update(extra or {})
    (folder / "mod.json").write_text(json.dumps(card, ensure_ascii=False), encoding="utf-8")
    return folder


def test_inject_before_main_and_uninject(tmp_path):
    from modkit import modstore
    game = make_core_game(tmp_path)
    store = tmp_path / "store"
    put_mod(store, "My Mod")

    original = (game / "Data" / "Scripts.rxdata").read_bytes()
    r = modstore.apply(store, "My Mod", game)
    assert r["did"] == "설치됨"
    assert modstore.installed(game) == ["My Mod"]
    assert (game / "Data" / "Scripts.rxdata.orig").read_bytes() == original

    modstore.apply(store, "My Mod", game)          # 두 번 눌러도 안 쌓인다
    assert modstore.installed(game) == ["My Mod"]

    modstore.remove("My Mod", game, store=store)
    assert modstore.installed(game) == []


def test_expects_mismatch_blocks(tmp_path):
    from modkit import modstore
    game = make_core_game(tmp_path)
    store = tmp_path / "store"
    put_mod(store, "Picky Mod", extra={"expects": {"Main": "0" * 32}})
    with pytest.raises(modstore.BaseChanged):
        modstore.apply(store, "Picky Mod", game)


def test_wrong_game_blocks(tmp_path):
    from modkit import modstore
    game = make_core_game(tmp_path)
    store = tmp_path / "store"
    put_mod(store, "Alien Mod", game="Other Game")
    (store / "Other Game").rename(store / "OtherDir")  # 폴더명은 무관, 카드의 game이 기준
    with pytest.raises(modstore.WrongGame):
        modstore.apply(store, "Alien Mod", game)
```

- [ ] **Step 2: 통과 확인** (이사가 온전하면 바로 초록이어야 한다)

Run: `uv run pytest tests/test_inject.py -v`
Expected: PASS 3건. 실패하면 이사 회귀 — Task 2를 의심.

- [ ] **Step 3: Commit**

```bash
git add tests/test_inject.py && git commit -m "test: 주입형 계약 회귀 그물 — MOD: 섹션 왕복·expects·게임 귀속"
```

### Task 4: fanlib을 modkit 소비자로 전환 (심)

**Files:**
- Create: `fangame-library/fanlib/_modkit.py`
- Modify: `fangame-library/fanlib/{modstore,modassets,modfit,scripts,rubyread,rubywrite}.py` — 본문을 심으로 교체
- Test: 기존 fanlib 동작 스모크

**Interfaces:**
- Produces: `import fanlib.modstore`가 modkit 모듈 그 자체를 돌려준다(`sys.modules` 치환). 기존 호출부(web.py·cli.py 등)는 무수정.

- [ ] **Step 1: 경로 연결자 작성**

`fanlib/_modkit.py`:

```python
"""modkit(모드 코어 정본)을 임포트 경로에 잇는다.

2026-08-03 모드 코어가 modkit sketch로 이사했다. fanlib의 동명 모듈들은
modkit을 그대로 내미는 심이다 — 수정은 modkit에 먼저.
"""
import os
import sys
from pathlib import Path

HOME = Path(os.environ.get("MODKIT") or Path.home()
            / "workspace" / "claude-native" / "sketches" / "modkit")
if not (HOME / "modkit" / "modstore.py").exists():
    raise ImportError(f"modkit을 찾지 못했어요: {HOME} — MODKIT 환경변수로 경로를 주세요")
if str(HOME) not in sys.path:
    sys.path.insert(0, str(HOME))
```

- [ ] **Step 2: 6개 모듈을 심으로 교체**

각 파일의 본문 전체를 아래 3줄로 (modstore 예 — 나머지 5개도 모듈명만 바꿔 동일):

```python
from . import _modkit  # noqa: F401 — modkit 경로 연결
import sys
import modkit.modstore
sys.modules[__name__] = modkit.modstore
```

modstore 심에만 한 줄 더 — fanlib의 기존 기본 보관소 유지:

```python
from pathlib import Path
modkit.modstore.DEFAULT_STORE = Path("/mnt/d/GameVault/mods")
```

- [ ] **Step 3: fanlib 스모크 — 실보관소 읽기와 웹 임포트**

```bash
cd ~/workspace/claude-native/sketches/fangame-library
uv run python -c "
from fanlib import modstore
mods = modstore.shelf()
print(len(mods), '개 모드'); print(sorted(m.name for m in mods)[:5])
import fanlib.web, fanlib.cli  # 무거운 호출부가 임포트만으로 안 깨지는지
print('web/cli import ok')"
```

Expected: 보관소 모드 수(현재 Z 모드 7개 내외 포함)와 `web/cli import ok`. 개수는 실행 시점 보관소 실측 기준으로 판단.

- [ ] **Step 4: fanlib pytest 있으면 실행**

Run: `uv run pytest -q` (fangame-library에서)
Expected: 이사 전과 같은 결과 (기존 실패가 있었다면 그 기존 실패만).

- [ ] **Step 5: Commit (fangame-library repo)**

```bash
git add fanlib/_modkit.py fanlib/{modstore,modassets,modfit,scripts,rubyread,rubywrite}.py
git commit -m "refactor: 모드 코어를 modkit으로 이사 — 6모듈을 sys.modules 심으로 교체"
```

### Task 5: 매니페스트 — 캡처와 진단

**Files:**
- Create: `modkit/manifest.py`
- Test: `tests/test_manifest.py`

**Interfaces:**
- Consumes: `modkit.modstore.shelf`, `read_mod` (카드의 assets `install_to`로 「아는 변경」 판정).
- Produces:
  - `capture(game_dir, game="", version="", exclude=None) -> dict` — `{"modkit_manifest": 1, "game", "version", "exclude": [...], "files": {상대경로: [size, crc32]}}`
  - `save(manifest, path)` / `load(path) -> dict`
  - `diagnose(game_dir, manifest, store=None) -> Diagnosis` — `@dataclass(frozen=True)`: `intact: tuple`, `known: tuple[tuple[str, str]]`(경로, 모드명), `foreign: tuple`, `missing: tuple`, `backups: tuple`
  - `DEFAULT_EXCLUDE: tuple` — 유저 데이터 글롭 패턴

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_manifest.py`:

```python
"""매니페스트 캡처와 4종 판정."""
import json
from pathlib import Path

import pytest

from tests.test_inject import make_core_game, put_mod


def test_capture_and_roundtrip(tmp_path):
    from modkit import manifest
    game = make_core_game(tmp_path)
    (game / "Graphics").mkdir(); (game / "Graphics" / "a.png").write_bytes(b"png")
    (game / "Saves").mkdir(); (game / "Saves" / "s1.rxdata").write_bytes(b"save")

    m = manifest.capture(game, game="Old Game", version="v1")
    assert m["modkit_manifest"] == 1
    assert "Graphics/a.png" in m["files"]
    assert "Data/Scripts.rxdata" in m["files"]
    assert not any(p.startswith("Saves/") for p in m["files"])  # 유저 데이터 제외

    manifest.save(m, tmp_path / "manifest.json")
    assert manifest.load(tmp_path / "manifest.json") == m


def test_diagnose_four_verdicts(tmp_path):
    from modkit import manifest, modstore
    game = make_core_game(tmp_path)
    (game / "Graphics").mkdir(); (game / "Graphics" / "a.png").write_bytes(b"png")
    m = manifest.capture(game, game="Old Game")

    store = tmp_path / "store"
    put_mod(store, "My Mod")
    modstore.apply(store, "My Mod", game)                      # 아는 변경 + .orig 백업
    (game / "Graphics" / "a.png").write_bytes(b"tampered")     # 외래 (변경)
    (game / "oldpatch.txt").write_bytes(b"trace")              # 외래 (추가)
    (game / "Game.ini").unlink()                               # 누락

    d = manifest.diagnose(game, m, store=store)
    assert ("Data/Scripts.rxdata", "My Mod") in d.known
    assert "Graphics/a.png" in d.foreign and "oldpatch.txt" in d.foreign
    assert "Game.ini" in d.missing
    assert "Data/Scripts.rxdata.orig" in d.backups
    assert "Data/Scripts.rxdata" not in d.foreign              # 아는 변경은 외래가 아니다


def test_diagnose_clean(tmp_path):
    from modkit import manifest
    game = make_core_game(tmp_path)
    m = manifest.capture(game, game="Old Game")
    d = manifest.diagnose(game, m)
    assert d.foreign == () and d.missing == ()
    assert set(d.intact) == set(m["files"])
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_manifest.py -v`
Expected: FAIL — `No module named 'modkit.manifest'`

- [ ] **Step 3: 구현**

`modkit/manifest.py` 핵심 구조:

```python
"""깨끗한 원본의 지문을 뜨고, 설치본을 대조해 넷으로 가른다.

판정: 원본 일치(intact) / 아는 변경(known — 보관소 카드가 설명) /
외래(foreign — 옛 패치 흔적) / 누락(missing). 도구 자신의 백업(.orig)은
backups로 따로 선다. 지문은 CRC32 — modassets의 replaces_crc와 한 벌이다.
"""
import fnmatch
import json
import zlib
from dataclasses import dataclass
from pathlib import Path

DEFAULT_EXCLUDE = (
    "Saves/*", "*.sav", "LastSave.dat", "*.ini.bak", "screenshot*",
    "_quarantine/*", "modkit-log.jsonl",
)
BACKUP_SUFFIXES = (".orig",)


@dataclass(frozen=True)
class Diagnosis:
    intact: tuple
    known: tuple    # (상대경로, 모드명)
    foreign: tuple
    missing: tuple
    backups: tuple


def _crc(path: Path) -> int:
    crc = 0
    with open(path, "rb") as f:
        while chunk := f.read(1 << 20):
            crc = zlib.crc32(chunk, crc)
    return crc


def _excluded(rel: str, patterns) -> bool:
    return any(fnmatch.fnmatch(rel, p) for p in patterns)


def capture(game_dir, game="", version="", exclude=None) -> dict:
    game_dir = Path(game_dir)
    patterns = tuple(exclude or DEFAULT_EXCLUDE)
    files = {}
    for p in sorted(game_dir.rglob("*")):
        if not p.is_file():
            continue
        rel = p.relative_to(game_dir).as_posix()
        if _excluded(rel, patterns) or rel.endswith(BACKUP_SUFFIXES):
            continue
        files[rel] = [p.stat().st_size, _crc(p)]
    return {"modkit_manifest": 1, "game": game, "version": version,
            "exclude": list(patterns), "files": files}
```

`diagnose()`는: ① 게임 폴더를 다시 훑어 (제외 패턴은 매니페스트에 든 것을 사용) 현재 파일 지도를 만들고, ② `store`가 있으면 `modstore.shelf(store, game=manifest["game"])`의 카드에서 「모드가 소유한 경로」 지도를 만들고 — 에셋의 `install_to` 전부 + 스크립트 모드가 건드리는 `Data/Scripts.rxdata`·`Data/PluginScripts.rxdata`(그 모드가 이 설치본에 설치돼 있을 때만, `modstore.installed`로 확인) — ③ 파일마다 순서대로 가른다: `.orig`류 → backups / 매니페스트와 크기·CRC 일치 → intact / 모드 소유 → known / 그 외 → foreign. 매니페스트에 있는데 현장에 없으면 missing. `save`/`load`는 `json.dumps(ensure_ascii=False, indent=1)` 왕복.

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_manifest.py -v`
Expected: PASS 3건

- [ ] **Step 5: Commit**

```bash
git add modkit/manifest.py tests/test_manifest.py
git commit -m "feat: 매니페스트 캡처·진단 — 4종 판정(intact/known/foreign/missing)"
```

### Task 6: 격리와 복원

**Files:**
- Modify: `modkit/manifest.py` (이어서)
- Test: `tests/test_quarantine.py`

**Interfaces:**
- Produces: `quarantine(game_dir, rel_paths, at=None) -> Path` — `_quarantine/<타임스탬프>/`로 상대경로 보존 이동, 그 폴더 반환. `restore(game_dir, stamp_dir) -> list` — 통째 원위치, 되돌린 경로 목록 반환.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_quarantine.py`:

```python
"""격리는 이동이지 삭제가 아니다 — 통째로 되돌릴 수 있어야 한다."""
from pathlib import Path

from tests.test_inject import make_core_game


def test_quarantine_and_restore(tmp_path):
    from modkit import manifest
    game = make_core_game(tmp_path)
    (game / "Graphics").mkdir()
    (game / "Graphics" / "old.png").write_bytes(b"old patch trace")
    (game / "oldpatch.txt").write_bytes(b"trace")

    box = manifest.quarantine(game, ["Graphics/old.png", "oldpatch.txt"],
                              at="2026-08-03T12:00:00")
    assert not (game / "oldpatch.txt").exists()
    assert (box / "Graphics" / "old.png").read_bytes() == b"old patch trace"
    assert box.parent == game / "_quarantine"

    back = manifest.restore(game, box)
    assert (game / "Graphics" / "old.png").read_bytes() == b"old patch trace"
    assert sorted(back) == ["Graphics/old.png", "oldpatch.txt"]
    assert not box.exists()  # 빈 격리 폴더는 걷는다


def test_quarantine_rejects_escape(tmp_path):
    from modkit import manifest
    import pytest
    game = make_core_game(tmp_path)
    with pytest.raises(ValueError):
        manifest.quarantine(game, ["../outside.txt"])
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_quarantine.py -v`
Expected: FAIL — `quarantine` 미정의

- [ ] **Step 3: 구현** — manifest.py에 추가

```python
def quarantine(game_dir, rel_paths, at: str | None = None) -> Path:
    """외래 파일을 게임 폴더 안 격리함으로 옮긴다. 지우는 일은 하지 않는다."""
    from . import gameinfo
    game_dir = Path(game_dir)
    stamp = (at or gameinfo.now()).replace(":", "-")
    box = game_dir / "_quarantine" / stamp
    moved = []
    for rel in rel_paths:
        src = (game_dir / rel).resolve()
        if not src.is_relative_to(game_dir.resolve()):
            raise ValueError(f"게임 폴더 밖이에요: {rel}")
        dst = box / rel
        dst.parent.mkdir(parents=True, exist_ok=True)
        src.rename(dst)
        moved.append(rel)
    return box


def restore(game_dir, box: Path) -> list:
    """격리함 하나를 통째로 원위치한다."""
    game_dir, box = Path(game_dir), Path(box)
    back = []
    for p in sorted(box.rglob("*")):
        if not p.is_file():
            continue
        rel = p.relative_to(box).as_posix()
        dst = game_dir / rel
        dst.parent.mkdir(parents=True, exist_ok=True)
        p.rename(dst)
        back.append(rel)
    for d in sorted(box.rglob("*"), reverse=True):
        d.rmdir()
    box.rmdir()
    return back
```

(주의: `rename`은 같은 볼륨 전제 — 격리함이 게임 폴더 안이라 항상 참.)

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_quarantine.py tests/test_manifest.py -v`
Expected: PASS 5건

- [ ] **Step 5: Commit**

```bash
git add modkit/manifest.py tests/test_quarantine.py
git commit -m "feat: 격리·복원 — 삭제 없는 이동, 통째 되돌리기"
```

### Task 7: 선언 검사 3겹 (declare)

**Files:**
- Create: `modkit/declare.py`
- Modify: `modkit/modstore.py` — `apply()` 어귀에 게이트, 묶음형 삽입 위치에 order 반영
- Test: `tests/test_declare.py`

**Interfaces:**
- Consumes: 카드의 선택 필드 `requires: list`, `conflicts: dict(모드명→사유)`, `order: {"after": [], "before": []}`, `touches: {"methods": [], "files": []}`.
- Produces:
  - `declare.Blocked(Exception)` — `.reasons: list`
  - `declare.gate(mod, installed_names, store) -> list` — requires·conflicts 검사, 위반이면 Blocked, 아니면 touches 겹침 경고 문자열 목록 반환
  - `declare.place(mod, installed_names) -> int` — order를 만족하는 삽입 인덱스(제약 없으면 `len(installed_names)`), 순환·모순이면 Blocked
  - `modstore.apply(store, name, game_dir, force=False)` — 게이트 통과 후 진행, `force=True`면 Blocked를 경고로 강등. 반환 dict에 `"warnings": list` 추가.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_declare.py`:

```python
"""선언 검사 3겹 — 차단(requires·conflicts), 배치(order), 경고(touches)."""
import pytest

from tests.test_inject import make_core_game, put_mod


def test_requires_blocks(tmp_path):
    from modkit import modstore, declare
    game = make_core_game(tmp_path)
    store = tmp_path / "store"
    put_mod(store, "Addon", extra={"requires": ["Base KR"]})
    with pytest.raises(declare.Blocked, match="Base KR"):
        modstore.apply(store, "Addon", game)


def test_conflicts_blocks_with_reason(tmp_path):
    from modkit import modstore, declare
    game = make_core_game(tmp_path)
    store = tmp_path / "store"
    put_mod(store, "Menu A")
    put_mod(store, "Menu B", extra={"conflicts": {"Menu A": "같은 씬을 통째로 갈아 끼움"}})
    modstore.apply(store, "Menu A", game)
    with pytest.raises(declare.Blocked, match="같은 씬"):
        modstore.apply(store, "Menu B", game)
    r = modstore.apply(store, "Menu B", game, force=True)  # 강행은 경고로
    assert any("Menu A" in w for w in r["warnings"])


def test_order_after_places_later(tmp_path):
    from modkit import modstore
    game = make_core_game(tmp_path)
    store = tmp_path / "store"
    put_mod(store, "Base KR")
    put_mod(store, "Labels", extra={"order": {"after": ["Base KR"]}})
    modstore.apply(store, "Labels", game)      # Base KR가 아직 없어도 얹힌다 (제약은 상대)
    modstore.apply(store, "Base KR", game)
    modstore.apply(store, "Labels", game)      # 다시 얹으면 after 제약이 실현된다
    names = modstore.installed(game)
    assert names.index("Base KR") < names.index("Labels")


def test_touches_overlap_warns(tmp_path):
    from modkit import modstore
    game = make_core_game(tmp_path)
    store = tmp_path / "store"
    put_mod(store, "Mod A", extra={"touches": {"methods": ["Scene_Map#update"]}})
    put_mod(store, "Mod B", extra={"touches": {"methods": ["Scene_Map#update"]}})
    modstore.apply(store, "Mod A", game)
    r = modstore.apply(store, "Mod B", game)
    assert any("Scene_Map#update" in w and "Mod A" in w for w in r["warnings"])
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_declare.py -v`
Expected: FAIL — `modkit.declare` 미정의

- [ ] **Step 3: declare.py 구현**

```python
"""제작자 선언을 적용 시점에 검사한다 — 차단, 배치, 경고 세 겹.

선언은 전부 선택이다. 선언 없는 옛 모드는 기계 감지(touches 겹침 경고)만 받는다.
"""


class Blocked(Exception):
    def __init__(self, reasons):
        self.reasons = list(reasons)
        super().__init__("; ".join(self.reasons))


def _card(store, name):
    from . import modstore
    try:
        mod = modstore.read_mod(store, name)
    except modstore.ModMissing:
        return {}
    import json
    return json.loads((mod.folder / "mod.json").read_text(encoding="utf-8"))


def gate(mod_card: dict, installed_names: list, store) -> list:
    """requires·conflicts를 검사하고, 통과하면 touches 겹침 경고 목록을 준다."""
    me = mod_card.get("name", "")
    blocks = []
    for need in mod_card.get("requires") or []:
        if need not in installed_names:
            blocks.append(f"`{me}`에는 `{need}`가 먼저 필요해요")
    for enemy, why in (mod_card.get("conflicts") or {}).items():
        if enemy in installed_names:
            blocks.append(f"`{enemy}`와 공존할 수 없어요 — {why}")
    for other in installed_names:
        other_card = _card(store, other)
        why = (other_card.get("conflicts") or {}).get(me)
        if why:
            blocks.append(f"`{other}`가 `{me}`를 거부해요 — {why}")
    if blocks:
        raise Blocked(blocks)

    mine = set((mod_card.get("touches") or {}).get("methods") or [])
    mine |= set((mod_card.get("touches") or {}).get("files") or [])
    warnings = []
    for other in installed_names:
        if other == me:
            continue
        theirs_t = _card(store, other).get("touches") or {}
        theirs = set(theirs_t.get("methods") or []) | set(theirs_t.get("files") or [])
        for spot in sorted(mine & theirs):
            warnings.append(
                f"`{me}`와 `{other}`가 모두 `{spot}`를 건드려요 — "
                "순서 선언이 없으면 나중 것이 이겨요")
    return warnings


def place(mod_card: dict, installed_names: list) -> int:
    """order 제약을 만족하는 삽입 인덱스. 모순이면 Blocked."""
    order = mod_card.get("order") or {}
    lo = max((installed_names.index(n) + 1 for n in order.get("after") or []
              if n in installed_names), default=0)
    hi = min((installed_names.index(n) for n in order.get("before") or []
              if n in installed_names), default=len(installed_names))
    if lo > hi:
        raise Blocked([f"order 제약이 모순이에요 — after {order.get('after')} / "
                       f"before {order.get('before')} (현재 {installed_names})"])
    return hi
```

- [ ] **Step 4: modstore.apply에 게이트 연결**

`apply(store, name, game_dir, force=False)`로 시그니처 확장. `read_mod` 직후·게임 귀속 검사 다음에:

```python
    import json as _json
    from . import declare
    card = _json.loads((mod.folder / CARD).read_text(encoding="utf-8"))
    try:
        already = installed(game_dir)
    except NoBundle:
        already = []
    warnings = []
    try:
        warnings = declare.gate(card, [n for n in already if n != mod.name], store)
    except declare.Blocked as no:
        if not force:
            raise
        warnings = [f"강행: {r}" for r in no.reasons]
```

묶음형 분기에서 새 설치일 때 `entries + [packed]` 대신 `declare.place`가 준 인덱스에 삽입 (인덱스는 모드 이름 리스트 기준이므로 그대로 배열 위치로 쓴다 — 묶음의 항목이 곧 모드 1:1). 주입형(`_inject`)은 현행 유지(Main 앞 일괄 — 모드 간 상대 순서는 걷어내고 다시 꽂는 재적용으로 실현되므로, `after` 제약 대상보다 먼저 설치된 경우 경고만 추가). 모든 반환 dict에 `"warnings": warnings` 추가.

- [ ] **Step 5: 통과 확인 + 전체 회귀**

Run: `uv run pytest -v`
Expected: PASS 전건 (기존 왕복 테스트 포함 — apply 시그니처 확장이 하위 호환인지 여기서 검증)

- [ ] **Step 6: Commit**

```bash
git add modkit/declare.py modkit/modstore.py tests/test_declare.py
git commit -m "feat: 선언 검사 3겹 — requires/conflicts 차단, order 배치, touches 경고"
```

### Task 8: harvest 시 touches 자동 초안

**Files:**
- Modify: `modkit/modstore.py` — `_lay_down` 끝(`_take_baseline` 부근)에서 카드에 touches 초안 기록
- Test: `tests/test_touches_draft.py`

**Interfaces:**
- Consumes: `modfit.take_baseline`이 계산하는 재정의 메서드 지도(키가 `"Class#method"` 꼴 — 실제 키 형식은 modfit.py를 읽고 그대로 따를 것).
- Produces: harvest 산출 카드에 `"touches": {"methods": [...], "files": [install_to...]}` — 이미 사람이 적은 touches가 있으면 보존(`_kept`와 같은 규율).

- [ ] **Step 1: 실패하는 테스트 작성**

```python
"""harvest가 touches 초안을 공짜로 채운다 — 사람 손 선언은 안 덮는다."""
import json
import zlib

from tests.test_import_standalone import make_game


def test_harvest_fills_touches_draft(tmp_path):
    from modkit import modstore, rubywrite
    game = make_game(tmp_path)
    # 코어(Scripts.rxdata)에 원본 메서드, 모드가 같은 메서드를 재정의하는 상황
    core = [[1, "Foo", zlib.compress(b"class Foo\r\n  def bar\r\n  end\r\nend\r\n")],
            [2, "Main", zlib.compress(b"# main\r\n")]]
    (game / "Data" / "Scripts.rxdata").write_bytes(rubywrite.dumps(core))
    store = tmp_path / "store"
    modstore.harvest(game, ["Base Mod"], store=store)

    card = json.loads((store / "Test Game" / "Base Mod" / "mod.json")
                      .read_text(encoding="utf-8"))
    assert "Foo#bar" in card["touches"]["methods"]


def test_harvest_keeps_manual_touches(tmp_path):
    from modkit import modstore
    game = make_game(tmp_path)
    store = tmp_path / "store"
    modstore.harvest(game, ["Base Mod"], store=store)
    card_path = store / "Test Game" / "Base Mod" / "mod.json"
    card = json.loads(card_path.read_text(encoding="utf-8"))
    card["touches"] = {"methods": ["Hand#written"], "files": []}
    card_path.write_text(json.dumps(card, ensure_ascii=False), encoding="utf-8")

    modstore.harvest(game, ["Base Mod"], store=store)  # 다시 꺼내도
    card = json.loads(card_path.read_text(encoding="utf-8"))
    assert card["touches"]["methods"] == ["Hand#written"]  # 손 선언 보존
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_touches_draft.py -v`
Expected: FAIL — 카드에 touches 없음

- [ ] **Step 3: 구현** — `_lay_down`에서 카드 쓰기 전에

```python
    kept_touches = _kept(folder / CARD, "touches", None)
```

카드 dict에 `"touches": kept_touches or _draft_touches(scripts, kept_assets)` 추가. 초안 함수:

```python
def _draft_touches(scripts, assets) -> dict:
    """제작자 선언의 초안 — 모드 소스에서 재정의 메서드를, 에셋에서 파일을 뽑는다.
    modfit의 클래스·메서드 파서를 그대로 쓴다(계산 규칙이 두 벌이 되면 어긋난다)."""
    from . import modfit
    methods = sorted({key for _, source in scripts
                      for key in modfit.method_map(source)})
    files = sorted(a.get("install_to", "") for a in assets if a.get("install_to"))
    return {"methods": methods, "files": files}
```

(참고: modfit에 소스→`Class#method` 지도를 만드는 내부 함수가 이미 있다 — 이름이 `method_map`이 아니면 실제 이름을 확인해 쓰고, private이면 공개 이름으로 승격한다. 파서를 새로 쓰지 말 것.)

- [ ] **Step 4: 통과 확인 + 전체 회귀**

Run: `uv run pytest -v`
Expected: PASS 전건

- [ ] **Step 5: Commit**

```bash
git add modkit/modstore.py tests/test_touches_draft.py
git commit -m "feat: harvest가 touches 초안 자동 기입 — 손 선언은 보존"
```

### Task 9: CLI

**Files:**
- Create: `modkit/cli.py`
- Modify: `pyproject.toml` — `[project.scripts] modkit = "modkit.cli:main"`
- Test: `tests/test_cli.py`

**Interfaces:**
- Produces: `modkit manifest <게임폴더> [-o out.json] [--game 이름] [--version 라벨]` · `modkit diagnose <게임폴더> -m manifest.json [--store 폴더] [--quarantine]` · `modkit harvest <게임폴더> <이름>... [--store]` · `modkit apply <이름> <게임폴더> [--store] [--force]` · `modkit remove <이름> <게임폴더> [--store]` · `modkit shelf [--store]`. 종료 코드: 성공 0, 차단·오류 1, 진단에서 외래 발견 2.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
"""CLI — 매니페스트·진단·격리 한 바퀴."""
import json
import subprocess
import sys

from tests.test_inject import make_core_game


def run_cli(*args):
    return subprocess.run([sys.executable, "-m", "modkit.cli", *map(str, args)],
                          capture_output=True, text=True)


def test_manifest_diagnose_quarantine_flow(tmp_path):
    game = make_core_game(tmp_path)
    out = tmp_path / "m.json"
    r = run_cli("manifest", game, "-o", out, "--game", "Old Game")
    assert r.returncode == 0 and out.is_file()

    (game / "oldpatch.txt").write_bytes(b"trace")
    r = run_cli("diagnose", game, "-m", out)
    assert r.returncode == 2 and "oldpatch.txt" in r.stdout

    r = run_cli("diagnose", game, "-m", out, "--quarantine")
    assert r.returncode == 0
    assert not (game / "oldpatch.txt").exists()
    assert list((game / "_quarantine").iterdir())  # 격리함에 들어갔다

    r = run_cli("diagnose", game, "-m", out)
    assert r.returncode == 0 and "깨끗" in r.stdout
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_cli.py -v`
Expected: FAIL — `modkit.cli` 미정의

- [ ] **Step 3: 구현** — argparse 서브커맨드, 각 핸들러는 코어 함수 호출 + 사람용 출력 몇 줄. `main(argv=None) -> int`와 `if __name__ == "__main__": sys.exit(main())`. diagnose 출력 예:

```
원본 일치 312 · 아는 변경 2 (My Mod) · 외래 1 · 누락 0
외래: oldpatch.txt
→ --quarantine로 격리하거나, 그대로 두려면 아무것도 안 해도 돼요.
```

`--quarantine`는 foreign 목록을 `manifest.quarantine`에 넘기고 격리함 경로를 찍는다. Blocked는 잡아서 사유를 stderr로, 종료 1.

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_cli.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add modkit/cli.py tests/test_cli.py pyproject.toml
git commit -m "feat: CLI — manifest/diagnose/harvest/apply/remove/shelf"
```

### Task 10: SPEC.md 초판

**Files:**
- Create: `SPEC.md` (modkit repo 루트)

**Interfaces:**
- Consumes: docs/design.md 7절의 목차, 그리고 Task 2~9가 실제로 구현한 동작(스펙은 구현이 지키는 계약의 기록 — 코드와 어긋나는 문장을 쓰지 않는다).

- [ ] **Step 1: 작성** — design.md 7절 목차 그대로 6개 절: ① 모드의 정의(폴더+mod.json, 배포 단위 zip) ② 카드 형식 v1 — 필드 사전을 표로(필수: name·game·scripts 또는 assets / 선택: engine·install·description·expects·meta·출처 필드·선언 필드 4종), 게임 귀속 원칙 ③ 설치 계약 3종과 검사 3겹(차단·배치·경고 — Task 7의 실동작 기준) ④ 안전 규약(.orig 규율·왕복 확인·원자적 교체·CRLF·격리) ⑤ 매니페스트 형식과 판정 4종(Task 5·6 실동작) ⑥ 호환성 검증(expects md5·베이스라인·판 올림 절차). 각 절에 대응 테스트 파일명을 각주로 달아 "스펙 문장 ↔ 검증 코드"를 잇는다.

- [ ] **Step 2: 대조 검토** — SPEC의 모든 필드명·판정명·명령이 코드와 일치하는지 grep으로 훑는다 (`grep -o '"[a-z_]*"' SPEC.md | sort -u` 대 카드 필드).

- [ ] **Step 3: Commit**

```bash
git add SPEC.md && git commit -m "docs: SPEC v1 — 카드·계약 3종·검사 3겹·매니페스트·격리"
```

### Task 11: 실물 검증 (Phase A 마감)

**Files:**
- Create: `docs/validation.md` (검증 기록)

**Interfaces:**
- Consumes: 실보관소 `/mnt/d/GameVault/mods`(읽기 전용으로만), 포켓몬 Z 설치본 사본, GameVault의 묶음형(PluginScripts.rxdata 있는) Essentials 게임 사본 하나.

- [ ] **Step 1: 사본에서 Z 주입 왕복** — 게임 폴더를 스크래치로 복사한 뒤 (원본 직접 조작 금지):

```bash
export MODKIT_STORE="/mnt/d/GameVault/mods/Pokemon Z Fangame"
uv run python -m modkit.cli shelf
uv run python -m modkit.cli remove "UI Text KR" <사본>
uv run python -m modkit.cli apply "UI Text KR" <사본>
```

apply 후 Scripts.rxdata가 fanlib 심 경유 결과와 바이트 동일한지 `md5sum` 대조. (보관소 레이아웃이 `<보관소>/<게임>/<모드>`이므로 store 인자 층위를 실측으로 확인 — 안 맞으면 CLI의 store 해석을 고친다.)

- [ ] **Step 2: 매니페스트 실측** — 깨끗한 Z 원본(V2.18)에서 `manifest` 캡처(수천 파일 수초 이내인지 시간 실측), v5 패치 덮인 사본을 `diagnose` — 패치 파일들이 foreign으로 뜨는 것이 정답(카드 없는 통파일이므로). 한글패치 통합 카드가 보관소에 있으니 `--store` 주면 에셋 대부분이 known으로 넘어가는지 확인.
- [ ] **Step 3: 묶음형 실물** — GameVault에서 PluginScripts.rxdata 있는 게임 하나 사본으로 harvest→remove→apply 왕복, 바이트 대조.
- [ ] **Step 4: 결과를 docs/validation.md에 기록** — 명령·출력 발췌·해시·소요 시간. 어긋난 것이 있으면 이슈로 적고 고친 뒤 재실측.
- [ ] **Step 5: Commit**

```bash
git add docs/validation.md && git commit -m "test: 실물 검증 — Z 주입 왕복 바이트 동일·매니페스트 실측·묶음형 왕복"
```

---

## Phase B — GUI·exe (Phase A 검수 후 착수)

Phase B 태스크는 굵게만 적는다 — Phase A가 끝나면 실측(픽업할 UI 자산, 윈도우 빌드 환경)을 반영해 세부 계획을 다시 뜬다.

### Task 12: pywebview GUI

`app.py` + `modkit/web/index.html`(단일 HTML, 인라인 CSS/JS). `webview.create_window`에 JS bridge 클래스(`Api.pick_folder` — `FOLDER_DIALOG`, `Api.diagnose`, `Api.apply_mod`, `Api.remove_mod`, `Api.import_zip`)를 물린다. 화면 세 걸음: 폴더 지정(최근 목록은 exe 옆 `modkit.json`에) → 진단 신호등([격리 후 진행] 기본 버튼) → 모드 서랍(zip 끌어놓기 → `mods/`에 해제). 행동 로그는 게임 폴더의 `modkit-log.jsonl`(진단 제외 목록에 이미 포함). 의존 추가: `pywebview`. 개발 실행 `uv run app.py`(WSL에선 GUI가 안 뜨므로 windows 호스트 파이썬으로 스모크).

### Task 13: exe 조립

`build.py` — 윈도우 호스트에서 PyInstaller `--onefile --windowed`. WSL이 아니라 Windows 파이썬으로 돌린다(PowerShell 절대경로 경유). 산출 `modkit.exe` 더블클릭 스모크: 진단→격리→얹기→빼기 한 바퀴 (사용자 실기 확인 포함).

### Task 14: 배포 준비

modkit repo GitHub 공개(README — SPEC 링크, exe 다운로드, 제작자 빠른 시작), Z v5 패치 zip에 매니페스트 동봉(pokemon-z make_package.py에 `manifest.json` 생성 단계 추가), 갤러리 안내문 초안.
