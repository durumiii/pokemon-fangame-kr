# 2026-08-18 — Z-53 ① 미등재 화자 채점: 산정부터 등재까지

티켓 [Z-53](../tickets/Z-53.md) 「남은 일」 ①의 실행 기록. 날짜 박제 — 고치지 않는다.

## 무엇을 했나

1. **산정** — 귀속표(`translate/data/speaker-attr.jsonl.gz`, 19,834행) 전수에서 말투 규칙
   미등재 자리를 다시 쟀다. 8-17 실측(1,037행) 이후 귀속표가 갱신돼(상속 규칙 수정
   `2c56a1c`·층 수리 `c883e89`) 1,400행·정체성 153개로 늘었고, 그중 이름도 그림도 빈
   무더기 둘(PS 407·PC 224행)을 빼면 **실 후보 151개·769행**.
2. **선별(A/B/C)** — 151개를 빌드가 실제 쓰는 매칭 함수(`resolve()` 직함 해석,
   `voice_sprite()` 어간 접기)에 통과시켜 갈랐다:
   **A 이미 덮임 42건**(239행, 정확 일치만 본 산정의 오탐 — 「아조스단 신병」이 이 부류) ·
   **B 합류 자명 9건**(72행) · **C 판정 필요 100건**(458행).
3. **B 합류** — acrilico3·5·82→acrilico2, enfermera→enfermera2, cripto2→cripto1,
   lunatico3→lunatico(groups 복사 항목), Alta Dama Malva→고위 귀부인 파키라,
   Auretosk→아우레토스크(voices 별칭 — canon의 기존 표시 번역 문자열을 그대로 키로),
   Carabineros→경관(canon 한 줄).
4. **C 채점** — 각 화자의 실제 대사(검수 재료 242장)를 읽고 초안 64건을 세웠고, 유지자가
   표로 일괄 승인했다(2026-08-18, 드라세나 여사제/전투호출 갈림 질의 후 「지금 그대로」).
   flag 36건은 초안 없이 갈림 사유만 세웠다.
5. **등재** — 초안 64건을 정본에 반영: groups.yaml 21항(잡담 스프라이트, B5 판정 5건 포함) ·
   voices.yaml 41항 · canon/names.jsonl 21줄(현행 표시 번역 실측으로 es→ko 매핑).
   64건 전부 `resolve()`/`personas()` 재호출로 매칭 True 실측. verify FAIL 0,
   gate emit 차이 0 유지. build.py로 dat 반영(값 차이 47곳, 왕복 검증 통과).
6. **귀속 오류 수리 3건** — overrides.jsonl: m490.e2.p0.c542(알카), m437.e1.p2.c358
   (내레이션→빈 화자), m342.e1.p0.c134(멜리아). 원인은 `speaker.py:68` TAG 정규식이
   `\sh` 연출 접두를 못 걷어 자기 이름표를 잃고 앞 화자를 상속한 것 — 전수 피해 조사는
   별도 진행.

## 등재 방식의 판정 둘 (근거는 voices 대장에도)

- **표시 이름이 겹치는 인물은 스페인어 원 이름표를 voices 키로 쓴다** —
  Revolucionaria/Revolucionario(둘 다 「혁명가」), Muchacha/Niña(둘 다 「소녀」).
  `voice_prompts()`가 group명 딕셔너리라 같은 한국어 이름 항목 둘은 뒤가 앞을 조용히
  덮는다(batch_pages.py 실측). resolve는 미매핑 이름을 그대로 돌려주므로 원 이름표 키로
  각자 지시를 가진다. canon 매핑은 넣지 않아 표시 번역은 그대로다.
- **전투호출 별칭(Tileo·Narciso·Drácena·EricLostie·Slowking)은 표시 번역을 바꾸지 않고**
  기존 표시 문자열 키의 별도 항목(+동일 인물 note) 또는 기존 항목 흡수로 잇는다 —
  파키라/고위 귀부인 파키라 선례.

## 처리로 닫은 것

- **울음·말줄임뿐이라 등재 불요 12건** — malvoPose·Pyroar·Morelull·Tlachtga·Bodhmall·
  Xerneas·de la Forja·trchar000_5~005_5. 말투 재료가 없어 규칙 대상이 아니다.
- **ATENCIÓN 1건** — 제작자 시스템 주의문. 인물 등재 대상 아님.
- **\PN 8행 중 6행** — 주인공이 실제로 이름표를 달고 서는 자리(대부분 말줄임)로 정상.
  오귀속 2행만 위 수리에 포함.
- **199·el Arma Definitiva** — 귀속 오류 아님(199는 who가 이미 Sabio로 정확, 병기
  이름은 전투호출 이름 인자 규칙대로).

## 남긴 꼬리 (티켓 Z-53에 등재)

스튜디오 판정 대기 7건(z53-flags) · 복합 이름 10건 등재 방식 · `\sh` 전수 조사(진행 중) ·
빈 화자 631행 원인 · 귀속표-정본 좌표 어긋남 36행 · gate refs 고아 값 3건(기존) ·
현자/야도킹 지시 통일 여부 · Ciudadanos 이름표 갈림 1행 · Sabio 층(PC) 잔존 ·
값 수정 창구 도구 pyyaml 헤더 점검(98dae04 후속).

## 재현 열쇠

산정·선별·초안 중간 산출은 세션 스크래치(`uncovered-identities.jsonl`·`triage-151.jsonl`·
`c100-drafts.jsonl`)에 있었고 소모품이라 repo에 남기지 않는다 — 등재 결과 자체가 정본
(voices.yaml·groups.yaml·canon/names.jsonl diff)이고, 검수 재료는
`translate/batch/z53-uncovered/`(전 151건)·`z53-flags/`(판정 대기 7건)에 있다.
