# Z-74 그림 자산 71장 한글화 — 제작·반입 (2026-08-19)

유지자가 실물 검수로 정한 범위 71장(목록 33 + rot류 38)을 전부 제작해 코어 모드에
반입했다. 시트 검수는 유지자가 마쳤고(「나머지는 완벽하다」), 등록 버튼 하나만
판독성 판정이 열려 있다.

## 파이프라인 (전부 repo, 재실행 동일 산출)

- 문안 원장: `translate/data/asset-texts.jsonl` 71행 — es(스페인어 원문, 수동 판독
  19 + gemini 3.7 flash vertex-flex 52) · ko(전량 유지자 검수 통과) · kind · in_core.
- 생성기 셋: `translate/assets/gen_cards.py`(카드 57 — 고운바탕 볼드, 클래스별 고정
  크기·테두리 자동 판별) · `gen_scenes.py`+`scenelib.py`(배경·스프라이트 8 — 연결
  성분 글자 제거) · `gen_parchment.py`(양피지 6 — 퀼트 재생성, 승인 시제와 픽셀 동일
  증명). 글꼴: 고운바탕(OFL)·갈무리 7/9/11/14(OFL).
- 반입: `translate/assets/install_assets.py` — 완성본을 코어 모드로 복사하고 신규
  25장을 mod.json assets에 등재(바닐라 CRC 실측). 기본 dry-run.
- 검수 시트: `share/review-cards/`(gitignore, 소모품).

## 반입 결과 (실측)

- 코어 모드: 교체 46 · 신규 25, mod.json 항목 +25(repo·보관소 동일 md5).
- modkit lint 오류 0 → apply 덮어씀(Controller UX와 bag류 그림 겹침 경고는 기존
  선언된 층 관계 그대로). 설치본 표본 4장 md5 일치, 신규분 `.orig` 백업 생성 확인.
- `verify.py` FAIL 0 · WARN 0.

## 문안에서 정본 쪽으로 흘러간 판정

카드 작업 중 게임 정본과의 통일이 여덟 건 일어났다 — 기계장치의 경이(붙여쓰기,
업적 2행) · 잊힌 섭정(업적·대사, 칭호 카드와 삼자 통일) · 겨울의 여백작(카드 승계).
그 밖의 칭호는 업적 자구를 승계했다(검의 달인 · 풋내기 요리사 · 마음을 옭아매는 자 ·
불꽃의 마녀 · 프로스트케이브 등).

## 남은 것

- 등록 버튼(icon_register) 판독성 판정 — 「등록」 유지 vs 원본 REG 환원. 한 파일
  교체로 되돌릴 수 있다.
- 실기 확인(게임 화면에서 카드·튜토리얼·상장 표시) — 사람 몫.
- 배포는 Z-34 합본 재배포에 합류.
