# 티켓 정리 — 넷 닫고 둘 합침 (2026-08-09)

유지자 기억(「Z-7·Z-11은 마무리, Z-15/Z-16은 제보자 오해」)을 실물로 대조해 닫는다.

## 닫음

- **Z-7 구세대 콘테스트 리본 표기** — v5.2.1 첫 배포에 이미 실렸다(CHANGELOG v5.2.1
  「PKHeX 리본표로 확정한 콘테스트 리본 30자리」). 실물 확인: `probe.py "Beauty Ribbon
  Ultra"` → dat·정본 모두 「뷰티리본울트라」, `verify.py check_ribbons()`가 지킨다.
  티켓은 문서 재편 때 원장 「미결」 절에서 기계적으로 옮겨져 낡은 채 남아 있던 것.
- **Z-11 Z-GUI 상태이상 아이콘 배포 경로** — v5.2.1 합본 zip에
  `Graphics/Pictures/battleStatuses.png`·`statuses.PNG`가 실려 있다(릴리스 자산 실측,
  로컬 dist와 sha256 일치 확인은 2026-08-09 Z-31 조사에서). 아이콘이 유저에게 닿는
  경로가 확정됐으므로 닫는다. 실기에서 보이는지 눈 확인만 Z-12 목록에 남는다.
- **Z-15 아이폰 Runa Constants.rxdata 소실 · Z-16 언어 선택 RandomObjects 크래시** —
  제보자 오해로 확정(유지자 전언, 2026-08-09): iOS 파일 관리자에서 **폴더 덮어쓰기(교체)**를
  해서 원본 게임의 `Data`가 통째로 사라진 설치 사고였다. 우리 가설(Z-15 「폴더 교체
  유력」)과 일치. 안내문·릴리스 본문의 파일 단위 덮어쓰기 문구는 v5.2.1 자산 교체판에
  이미 들어갔다. 패치 결함이 아니므로 둘 다 닫는다.

## 합침

- **Z-32 → Z-31** — 신형 루비 호환 층(심·수술·감사 도구)과 모바일 튕김 수리는 같은
  재배포에 실리는 한 묶음이라 「모바일 실행기 호환」 티켓 하나로 합쳤다. 로컬 mkxp-z
  시험대(P-2) 항목도 그 안에 절로 세웠다.
- **Z-29 → Z-5** — 말투표 잔여 둘(이름 키 불일치 2건 · `register.axis()` 호칭 오판)을
  조연 말투 등재 티켓의 절로 옮겼다. 둘 다 말투표(`ledger/voices.md` ·
  `persona-table.jsonl`)를 만지는 같은 작업이다.

## 같은 날 2차 (유지자 지시)

- **Z-12 실기 확인 묶음 닫음** — 유지자가 실기에서 목록 전체를 확인했다(전언,
  2026-08-09). 조이기 자리표시자의 「실기 전 손대지 말 것」 가드도 함께 풀렸다.
- **Z-23 → Z-18** — 조이기 주어 방향(구 Z-23)과 소금절이 폴백(구 Z-18)은 원작
  `PokeBattle_MoveEffects`의 같은 조임 문구 블록(5957행 템플릿 · 5969행 else 폴백)
  이라 「조임 기술 상태 문구 둘」 한 티켓으로 합쳤다.
- **로드맵 설명 전면 개서** — 근거 문서를 열지 않아도 무슨 일인지 읽히게 각 행을
  다시 썼다(유지자 지적: 「저대로는 근거 타고 들어가기 전엔 무슨 일인지 알 수 없다」).
