# Z-46 — 층 배정 도구 결함 둘 (P-3, 작음)

2026-08-13 전수 대조 감사가 실물로 확인한 결함 둘. 같은 도구(층 배정)라 한 티켓.
근거: [감사 보고](../log/research/2026-08-13-classification-audit.md).

## 할 일

1. **행수 문턱에 말투 정본 등재 우선 예외** — 인물 층 판정이 「이름표 50행 이상」인데
   voices.md 등재 인물 다섯(Anturia · Capitán Merlot · Barquero · Nácar · Mimi)이
   행수 미달로 잡담·기능 층에 떨어져 있다. 정본 명단 등재면 행수 무관 인물 층.
   selftest 케이스 추가.
2. **어간 축약 충돌 수정** — `stem("flareow")`→`flare`가 인물 명단과 우연 일치해
   아조스단 무명 단원이 인물 층으로 승격된다(`flaraow` 동일). 원시 이름 정확 일치를
   어간보다 먼저 보게 하고, **전 스프라이트 어간 충돌 쌍 전수 검사**를 붙인다.
3. 수선 뒤 귀속표 재산출 — 층 수치(인물 8,066 / 잡담·기능 5,927 / 지문·시스템 3,088)가
   움직인다. 규모 브리핑·지침의 수치 갱신, events-and-speech 「알려진 구멍」 절에서
   해당 항목 제거.
4. 확인: flareow류가 이미 인물 대우로 번역·판정을 받은 자리가 있는지.

## 진행 (2026-08-13, 가지 `worktree-z46-scene-axes`)

`speaker.py`에 `cls`·`once`·`flags` 세 칸을 배선하면서 결함 둘을 고쳤다. 기존 칸은
안 건드렸다. 재산출 19,310행, `verify.py --strict` FAIL 0 · WARN 0, `selftest` 19/19.

- 정본 명단은 **`docs/ledger/voices.md`의 인물 표**(60명)로 잡았다. `voice-prompts.jsonl`
  쪽은 `Aldeana`·`Gente` 같은 무리 이름표 지시까지 담고 있어 명단으로 쓰면 행인 1행짜리가
  정본 인물이 된다(그 판으로 돌렸을 때 PS가 735페이지로 부풀었다). 지목된 다섯은 PS로 선다.
- 어간 충돌 전수 검사(`speaker.py`의 `stem_conflicts`)는 셋을 집었다 — `flare`·`flara`에
  더해 `luigi`(마리오 카메오). `selftest`가 같은 검사를 다시 돌려 새 충돌을 떨어뜨린다.
- 층 수치(대사 17,081행): PS 682페이지 8,483행 · PC 2,218페이지 5,389행 ·
  N 1,802페이지 3,209행. 옛 수치는 606/8,066 · 2,305/5,927 · 1,791/3,088.
- `once`는 감사 박제(`2026-08-13-audit-cells.jsonl`) 649페이지와 **전부 일치**하고,
  `flags`는 645/649다. 어긋난 넷은 감사가 판 모드 플래그(759·430)를 뺀 자리와 스위치 91
  한 건이라, 이쪽이 §5.1 정의(어딘가의 페이지 조건으로 쓰이는 전역 스위치)를 따른 값이다.
- 4번 확인 결과: `flareow`·`flaraow`는 맵441 여섯 자리 9행이고 전부 이름표 없는 그림
  귀속이다. `fixlog`·`fixnotes`·`protected`·`approved`·`divergence-allowed` 어디에도
  없지만 **NPC 배치를 한 번 지나 정본에 반영됐다**(`batch/npc-out/n0434.jsonl`).
  그때 실린 화자 지시는 지금 다시 만들어 봐도 「명명 인물 「flare」」다 — `batch_npc.npc_of`가
  `sprite-groups.json`의 voices 어간을 그대로 보기 때문이라 이 티켓의 수선 밖에 남아 있다.

## 판정 (2026-08-13, 유지자)

- **luigi(마리오 카메오) = PC 확정** — 이름표 없는 카메오 단역, 페르소나표 관할.
- **batch_npc 잔존 경로는 고치지 않는다** — Z-47 착수 시 `batch_pages --npc`로
  일원화하며 batch_npc를 은퇴시키는 쪽으로 해소(Z-47에 등재). 그때까지 batch_npc를
  새로 돌릴 계획이 없어 방치 비용 0.
- 남은 것: 지문·시스템 층 121행 증가의 구성 규명(조사 배정됨 — 말하는 정령=인물
  판정은 quality 대장 등재).
