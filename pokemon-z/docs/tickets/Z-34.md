# Z-34 — 합본 재배포 (모드 릴리스 v2.1 이후)

모드 채널(mods-z-v2.1, 2026-08-09)로는 나갔고, 합본 zip 세 갈래는 아직 옛 판
(v5.2.1)이다. 실릴 것: 신형 루비 호환 층 + 박힌 문구 수술 28곳 + 최신 번역표 +
글꼴 수리(굵은 한글 튕김·갈무리 라틴·?!). PC 실기 확인은 2026-08-09 통과
(경위는 [Z-31 박제](../log/Z-31.md)).

## 유지자 판정 (2026-08-09)

- 새 모드 둘 중 **Type Matchup**은 세 갈래 합본에 기본 동봉, **Debug Toggle**은
  디버그 추가판에 싣고 그 판의 기존 디버그 편집을 대신한다.
- **모드 이름 끝의 `Z`를 뗀다** — `Type Matchup Z`·`Debug Toggle Z` → `Type Matchup`·
  `Debug Toggle`(저장소·보관소·설치본의 주입 섹션 이름 모두).
- 디버그판은 **켜진 채로 나가고 토글이 그 위에 얹힌다** — Debug Toggle의 키 폴링은
  Win32API라 모바일에서 안 먹어서, 단순 대체하면 Runa에서 디버그를 못 연다.
- 배포 전에 **mkxp-z 로컬 시험대를 세운다**(아래).

## 됨 (2026-08-09)

- 이름 바꿈 · 보관소 폴더와 카드 · 게임 설치본의 주입 섹션 이름까지.
- `share/make_package.py` — 합본 주입 목록에 Type Matchup, 디버그판은
  Debug Toggle + `patch_debug.py --only Main`(새 옵션), 판 번호 v5.3, 합본 카드의
  `conflicts`에 Type Matchup. `expects`가 순정 기준인 모드는 `FORCE_INJECT`로 넘긴다.
- 한글패치 코어 재조립(정본 최신 반영 — 값 차이 67곳·새 키 13개).
- zip 네 벌 — `share/dist/pokemon-z-kr-patch-v5.3_{1-galmuri,2-dppt,3-bw,4-debug-add}.zip`.
  디버그판 확인: Main 머리 `$DEBUG = true`, heal alias 한 겹, MOD 표 없음.

## mkxp-z 시험대 (P-2) — 섰고, 곧바로 하나 잡았다

자리: `D:\Game\_probe\z-mkxpz`(게임 사본 + v5.3 DPPt 덮음). 런타임은 Pokemon
Wishing Star v1.0.8이 싣고 있던 mkxp-z(루비 3.1) 셋 — `Game.exe`·
`x64-msvcrt-ruby310.dll`·`zlib1.dll`·`soundfont.sf2`. 오류는 화면 없이도 읽힌다:
`Start-Process -RedirectStandardError`로 받은 `D:\Game\_probe\z-err.txt`에
같은 문장이 그대로 찍힌다.

시험대만의 손질 둘(배포물에는 안 들어간다):
- 32비트 RGSS 전용 dll 다섯을 `_rgss-only-dlls/`로 치웠다 — 64비트 mkxp-z가
  `gif.dll`을 못 얹어 죽는다. 모바일에는 이 파일들이 없다.
- `z-bench-preload.rb`(mkxp.json의 `preloadScript`)가 `Win32API`를 늘 0을 돌려주는
  껍데기로 갈아 끼운다 — 안 그러면 `getUnicodeString`의 `RtlMoveMemory` 포인터
  장난이 64비트에서 세그폴트다(`SpriteWindow:421`).

**잡힌 것 — 한국어를 고르면 SyntaxError로 죽는다.**
`invalid multibyte escape: /"[^"\\\n\r\x00-\x1f\x7f-\x9f]*"/` — `pbIsJsonString`의
정규식 리터럴이다(`PSystem_Utilities NUEVO:199` 자리, 실제로 죽는 곳은
`PSystem_System`이 `Data/Constants.rxdata`를 `eval`하는 대목). 루비 3.x는 UTF-8
소스의 정규식 리터럴에 `\x7f`~`\x9f` 바이트 escape를 허용하지 않는다.

죽는 자리가 게임 제 파일(`Data/Constants.rxdata` — 한글패치가 안 건드린다)이라
**v5.2.1도 같을 것**으로 본다(추정 — 옛 판으로 같은 시험대를 안 세워 봤다). 그렇다면
이번 판을 막을 사유는 아니고, 따로 판정할 일이다.

아직 모르는 것: 이것이 **순정에서도 나는가**(그렇다면 Runa 사용자는 왜 지금
쓰고 있나 — Runa가 스크립트를 다른 인코딩으로 평가하는지), 스페인어 경로도
같은지. 순정 사본에 같은 시험대를 세워 견주는 것이 다음 수다. 고칠 자리는
1.8.7과 3.x 양쪽에서 서야 하므로 `\u` 표기는 못 쓴다.

## 남음

- 위 SyntaxError의 범위 판정과 수술 — 배포를 막을지 여기서 갈린다.
- 릴리스 노트를 태그 diff에서 뽑고([release](../guides/release.md)) **유지자 승인 후** 릴리스.
- 옛 판의 `fontSub`를 걷으려 싣던 원본 `mkxp.json`은 v5.3에도 그대로 실었다 —
  v5.1에서 곧장 올라오는 사람이 아직 있어서다. 뺄 판을 따로 정한다.
