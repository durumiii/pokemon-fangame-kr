# Z-63 — PBS 층: 트레이너가 붙인 포켓몬 별명이 번역 경로 밖에 있다

**단계** P-1(실측) · **크기** 작음

배틀에서 간수 피노의 대포무노가 `Paulie`로 뜬다. 유지자 제보 2026-08-16,
표기는 **「폴리」로 통일**(유지자 판정 2026-08-16 — 대사 쪽은 이미 그 값으로 서 있다).

## 지금까지 확인한 것 (조사 보고, 실측)

- 별명이 사는 자리는 게임 설치본 `PBS/trainers.txt:1464`:

      OCTILLERY,57,,HYDROPUMP,BULLETSEED,ICEBEAM,FLAMETHROWER,,,,,,,,Paulie

  바로 위 헤더가 `PINOT` / `Pinot` / `4`라 피노의 네 번째 포켓몬이 맞다.
- `Scripts.rxdata` 277섹션 전수 grep에서 `Paulie`는 0건 — 스크립트 쪽에는 없다.
- **저장소에 PBS를 다루는 도구가 하나도 없다**(`translate/`·`share/` grep 결과).
  텍스트 파이프라인의 여섯 층 어디에도 안 걸리는 층이다.

## 할 일

1. PBS 층을 지침(`docs/guides/text-pipeline.md`의 층 표)에 등재한다 — 지금 표에
   자리가 없어 다음 사람이 같은 조사를 반복한다.
2. `trainers.txt`에 미번역 별명이 더 있는지 전수로 센다(조사는 이 한 건만 봤다).
3. 고치는 절차를 세운다 — 설치본 `PBS/trainers.txt`를 고치고 게임의 디버그 컴파일러로
   `trainers.dat`를 다시 굽는 길이 후보다. 재주입·재현이 되는 꼴(멱등)로 만든다.

## 미확인

PBS 재컴파일이 `trainers.dat`만 갱신하고 다른 부작용이 없는지는 실측이 아니라
Essentials 표준 동작에 기댄 추정이다 — 1차로 이것부터 잰다.
