# Z-58 — 「분기다름」이 배치 사정권에서 빠진다 (닫힘 2026-08-19)

**유지자 판정 ① 통째 편입 — 반영 완료.** `structure.voice_ref`가 분기다름을 태그·상속과
같이 voices로 보낸다(구현·측정은 아래 실측 절에 이어 붙임). plan 실측: 111→114페이지 ·
340→372행(순증 +34행, 재접힘 2행은 dedupe 대표 이동으로 covers에 보존 — 유실 0).
분기 갈래에 섞인 시스템 진행 문구가 인물 목소리 지시를 받는 위험은 **현행 태그·상속도
같은 결이라, 지문 갈래 휴리스틱(Z-53 ② 꼬리 nkind)이 서면 한번에 거른다**(유지자).

주연 갈래 배치 게이트가 귀속법을 `태그`·`상속`만 받는다(`translate/batch_pages.py`).
그런데 조건 분기의 갈래마다 화자가 갈리는 자리에 붙는 `분기다름` 203행이 **사람 대사인데
통째로 빠진다.**

2026-08-14 이름표 전수 조사가 곁다리로 찾았다
([기록](../log/research/2026-08-14-tag-whitelist.md) 「곁다리로 나온 별개 구멍」).
층(`cls`)과 무관한 문제라 그때 안 건드렸다.

## 할 일

- 203행이 실제로 사람 대사인지 표본으로 확인한다(귀속법이 `분기다름`이어도 지문일 수 있다).
- 게이트에 `분기다름`을 더할지, 아니면 그 안에서 더 가를지 판정 재료를 갖춰 올린다.
- 사정권이 넓어지면 배치 비용이 는다 — 늘어나는 행 수를 함께 적는다.

## 2026-08-19 — P-2 실측: 표본 판독과 사정권 델타 (판정 대기)

모집단이 움직였다 — 귀속표의 `how="분기다름"`은 지금 **507행**이다(티켓 머리의 203행은
8-14 조사 시점 수. 8-18 상속 개편의 재분류를 지났다). 전부 kind=text·who 있음, 76맵.

- **표본 25행 판독(seed 53): 20행은 명백한 사람 대사, 5행은 시스템·지문이다** —
  랜덤 모드 안내(m66.e1), 「전기 에너지가 주입되었다」(m84.e4), 기술 떠올리기
  창구(m5.e27) 류. 분기 갈래에 화자가 갈리는 자리라 진행 문구가 섞인다.
- **사정권 델타: 새로 열리는 페이지가 0이다.** SYS(Contrincante 22)·어투 확정 32를
  거른 통과 후보는 **453행 · 127페이지**인데, 127페이지 전부에 태그·상속 행이 이미
  있다 — 편입해도 이미 배치에 드는 페이지에 행이 얹힐 뿐 장면 수는 안 는다.
- **층 판정은 시스템 섞임을 못 거른다** — 표본의 시스템 줄 페이지 다섯 다 layer가
  PS/PC다(진짜 대사가 같이 사는 페이지라 페이지 판정으로는 맞다). 행 단위 갈래가
  필요한 자리다.
- 구멍의 실물은 Z-53 ② 이후 **로더 한 줄**이다 — `structure.voice_ref`가 태그·상속만
  voices로 보내고 분기다름은 어느 표에도 안 보낸다(structure.py:98). 고치는 자리도
  여기 하나이고, 소비자 전부(batch_pages 갈래 셋·materials·scene_eval)에 같이 먹는다.

**판정거리** — ① 통째 편입: 453행이 사정권에 들고 비용 증가는 행뿐. 위험은 표본
기준 20%가량의 시스템 줄이 인물 목소리 지시를 받는 것. ② 편입하되 행 단위로 더
가른다: 시스템 줄 갈래(진행 문구·창구)를 걸러낼 신호가 필요하다 — 새 휴리스틱이라
정답 표본 채점이 선행(전역 규율). ③ 현상 유지: 453행의 사람 대사가 계속 배치 밖.

## 2026-08-18 — 상속 규칙이 방향을 가른다 (줄기 A)

`분기다름`의 정의가 좁아졌다. 이름표보다 깊은 줄만 낮은 확신으로 물려받고, 얕은 줄
(분기 합류 꼬리)은 방금 닫힌 가지들의 이름표가 **한 사람일 때만** 잇는다(성별 분기류,
맵97 뱃사공). 여럿이면 끊는다 — 맵418 재도전 창구 문구가 도전자 이름을 받던 결함의
근치(줄기 B 제보). 재분류: who가 바뀐 것은 맵418 계열 25행뿐(전부 미상으로), 39행은
상속으로 승격, 층 변화 0. 정답 자리 `KNOWN_JOIN` 둘이 selftest에 섰다.
