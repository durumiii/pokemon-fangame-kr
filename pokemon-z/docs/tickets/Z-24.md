# Z-24 — 스크립트에 박힌 한국어 70행: 수술 계획

전수 목록: [script-baked-korean](../log/research/2026-08-08-script-baked-korean.md).
번역표로 못 고치는 자리라 코어의 `Scripts.rxdata`를 직접 수술한다. 치환 자구는
2026-08-09 본가 대조 실측([scout](../log/research/2026-08-09-z21-z24-z30-scout.md))
기반이고, 유지자 판정을 같은 날 받았다 — 아래 표가 승인된 계획이다.

## 기제

`share/patch_ruby_compat.py`(Z-31) 선례를 그대로 따르는 치환 도구를 만든다.

- 치환은 **절 이름 + 줄 번호 + 기존 문자열 완전 일치**로만 한다 — 하나라도 안 맞으면
  그 항목은 건너뛰고 보고한다(멱등, 추측 치환 없음).
- 치환표는 도구 안이 아니라 별도 JSONL(절·줄·옛 문자열·새 문자열·근거 태그)로 둔다.
- 대상: 보관소 기반판(`/mnt/d/GameVault/.../한글패치 코어/Data/Scripts.rxdata`) +
  설치본. 수술 전 `.pre-baked.bak` 백업(compat 선례와 같음).
- 검증: 수술 후 전수 추출을 다시 돌려 의도대로 바뀌었는지 대조 +
  `share/qa-ruby-compat.py`로 문법 불통 0 + 비대상 절 무손상 확인.
- 재배포는 Z-31과 같은 코어 재굽기를 타므로 **Z-31 재배포에 합류**한다.

## 치환표 (유지자 승인 2026-08-09)

| 절:줄 | 지금 | 바꿀 것 | 근거 |
|---|---|---|---|
| `PokeBattle_MoveEffects`:11·25 | 하지만 빗나갔다! | 그러나 실패하고 말았다! | **의미 오류** — 문맥이 miss가 아니라 fail(미구현/실패 기술 폴백). 본가 「¡Pero ha fallado!」 쌍 |
| `PokeBattle_Battle`:2602 | 폭우가 그쳤다! | 강한 비가 그쳤다! | HEAVYRAIN 종료 분기. 본가 za btl_std |
| `PokeBattle_Battle`:2613 | 햇살이 원래대로 돌아왔다! | 햇살이 원래대로 되돌아왔다! | HARSHSUN 종료. 본가 za btl_std |
| `PokeBattle_Battle`:2624 | 기묘한 난기류가 잦아들었다! | 수수께끼의 난기류가 가라앉았다! | STRONGWINDS 종료. 본가 za btl_std |
| `PItem_ItemEffects`:726·747 | 능력이 올랐다! | 능력이 올라갔다! | 본가 능력치 문구는 일관되게 「올라갔다」 |
| `PScreen_Options`:751 | …셋 모드는 난이도가 올라간다. | …토너먼트 모드는 난이도가 올라간다. | 옵션 값 표기(Fijo→토너먼트)와 정합 |
| `PScreen_Options`:755 | …수직동기화를 꺼라. … | …수직동기화를 끈다. … | 명령형 → 이웃 설명문과 같은 평서 |
| `Monotype`:72·74 | 새 스타팅 포켓몬을 골라라. | 새 스타팅 포켓몬을 고르자. | 명령형 제거(자구는 반영 때 유지자 확인) |
| `PokeBattle_Scene`:2371 | 훔치는 건 도둑이나 하는 짓이다. | 남의 것에 손대면 도둑! | 본가 la·za btl_std |
| `Diploma Nuz1`:7 | 눌록을 완주했음을 증명하는 상장. | 너즐록을 완주했음을 증명하는 상장. | 정본 표기 「너즐록」(07·09절 일관). glossary 등재 예정 |
| `Diploma Nuz2`:7 | 하드 눌록을 … | 하드 너즐록을 … | 〃. ⚠ 도구 설명문(09절 945행)은 같은 물건을 「영웅 너즐록」이라 부른다(es heroico) — 하드/영웅 어긋남은 반영 때 확인 |

## 그대로 두는 것 (같은 날 판정)

- **성격 25종**(PScreen_Summary:342) — 화면 틀 「{1} 성격.」에는 지금의 형용사형이
  맞다(본가 트레이너 메모와 21/25 일치). 4종의 자구 차이도 손대지 않는다.
- **Export to Showdown 존대 두 줄** — 존대 유지. 전체 말투 통일은 하지 않고
  명령형(꺼라·골라라)만 고친다.
- `Sacar Equipo`:16 「포켓몬을 골라야 한다!」 · BugContest 방송 존대 — 유지.
- `Editor`:2498·2696(개발 화면) · `Settings`:352(「한국어」) — 유저 무관/정답.
- 23절 1757행 `"Set"→"교체"` 우려는 해소 — 코어 270절 전수에서 `"Set"` 리터럴
  사용처 없음(실측 1회). 죽은 키로 보인다.

## 반영 완료 (2026-08-09)

`share/patch_baked_korean.py` + `share/baked-korean-fixes.jsonl`(14곳)로 보관소
기반판·설치본 양쪽 수술 완료 — 백업 대조로 치환표 밖 무변경, 루비 3.4.5 문법
불통 0, 재실행 멱등 확인. 같은 날 코어 재굽기(`runa/make-patch-mod.py`)로 보관소
모드에 편입 완료 — 남은 것은 합본 재조립·재배포(Z-31)뿐이다.
