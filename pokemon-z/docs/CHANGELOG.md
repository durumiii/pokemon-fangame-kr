# 변경 이력

배포한 판과 그 판에 들어간 것. **아직 안 낸 것은 「미공개」에 쌓고, 낼 때 판 번호를 붙여
옮긴다.** 릴리스 노트 본문(플레이어가 읽는 글)은 `docs/log/research/`에 판마다 따로 있고,
여기는 유지자가 훑는 목록이다.

## 모드 릴리스 mods-z-v2.1 (2026-08-09)

모드 채널(durumiii/essentials-modkit)로만 낸 판 — 합본 zip은 아직 안 냈다.
한글패치 코어(신형 루비 호환 층 + 박힌 문구 수술 28곳 + 최신 번역표)와 글꼴 셋
(굵은 한글 튕김 수리, 갈무리 BW 라틴 통일·?! 위치)이 실렸다. 이로써 Z-21(악센트
글자 크기)·Z-24(박힌 한국어)·Z-31(모바일 호환 묶음)을 닫았다 — 합본 몫은 Z-34로. 아래 「미공개」의
정본 작업도 이 코어의 번역표에 실려 나갔다.

## 미공개

지금 정본에는 들어갔지만 아직 합본으로 배포하지 않은 것.

- **Z-73을 닫았다(2026-08-18).** 문구 조회를 좌표로 여는 일 — 조회 기준을 이벤트 소속
  맵으로 내리고, 전투 호출 대사와 상점 점원 갈래에 좌표·갈래 열쇠를 냈다(좌표 정본 54줄,
  루비 오라클 통과). 세부는 [기록](log/Z-73.md). 유지자 실기 확인 두 자리(맵26 검은깃털
  상점 반말 · 맵25 브리오프 하게체)만 남아 있고, 어긋나면 제보 경로로 돌아온다.

- **상점 점원 문구를 한 결로 맞추고 갈래 셋을 정했다(2026-08-18).** 상점 화면에서 점원
  목소리로 나가는 열한 줄이 존대 다섯·반말 여섯으로 섞여 있던 것을 기본 갈래(존대)로
  통일했다(여섯 줄 수정). 반말 일곱 상점과 하게체 한 상점은 갈래 열쇠로 갈랐다 — 값은
  `krmart:<갈래>|<원문>`, 배정은 `krmart-at:<맵>:<이벤트>`로 둘 다 절23에 살아, 상점을 더
  가르는 데 소스 수술이 다시 필요 없다. 문안 스물두 줄·배정 여덟 줄·검사 하나가 섰고
  루비 오라클로 arm 넷을 쟀다([기록](log/2026-08-18-mart-register.md)). 실기 확인만 남았다.
  곁가지로 이 자리의 문구로 알던 「어서 오세요!」가 **죽은 키**였음이 드러났다.
- **판정 재료가 맵 절 밖도 다룬다(2026-08-18).** 상점·창구처럼 이벤트에서 열리는 화면의
  문구를 앵커로 그 이벤트의 겪는 순서 뒤에 붙여, 플레이어가 말을 건 뒤 무엇을 어떤
  차례로 만나는지가 한 장에 선다. 제안 문안은 묶음별로 갈려 같은 자리에 갈래별 문안을
  올릴 수 있고, 화면 자리의 판정 열쇠에는 앵커가 붙는다(안 붙이면 갈래별 판정이 서로를
  덮는다). 쓰는 법은 [text-pipeline](guides/text-pipeline.md) 「판정 재료와 판정 요청」.

- **페르소나·스프라이트 묶음 정본이 stage0로 내려왔다(2026-08-18, Z-53 줄기 A 3일차).**
  `stage0/groups.yaml`이 직접 편집하는 정본이 되고 옛 persona-table.jsonl·
  sprite-groups.json은 은퇴했다(소비자 일곱 이전, 파생값 아홉 전후 동치 증명).
  배포 kit도 groups.yaml을 싣고, 웹 찾아보기 화자 표가 낡아 있던 둘(다친 사냥꾼 층
  이동·「파파 로토」 표기)을 따라잡았다.

- **교환·배틀포인트 창구를 화자별로 갈랐다(2026-08-18, 이벤트 열다섯 · 82줄).** 카페와
  호텔에서 서너 스프라이트가 같은 안내문을 나눠 읽는 자리다. 정본 키가 (맵, 원문)이라
  한 맵에 값이 하나뿐이어서 2026-08-08에 맵 다수결로 눌러 두었는데, 좌표 조회가 서면서
  그 전제가 사라져 유지자 판정으로 페르소나대로 갈랐다. 가르는 단위는 이벤트 통째다 —
  안내문만 갈면 그 NPC가 제 교환 제안과 격이 어긋난다. 문안은 이미 정본에 서 있는 반대
  격 판을 옮겼고 NPC 고유 줄은 어미만 갈았다. 새로 쓴 것은 배틀포인트 창구 다섯 줄뿐이고
  (이 게임의 배틀포인트 창구가 전부 해요체라 반말판이 없었다), 뤼미에르 대호텔은 맵405·429
  두 판의 같은 직원이 함께 간다. 정본 44줄 · 좌표 38줄이고 통일 셋을 갈림 허용으로 옮겼다.
  판정은 [voices 대장](ledger/voices.md) 맨 위 항목.

- **맵 대사 조회가 이벤트 소속 맵을 본다(2026-08-18, Z-73 잔여).** 지금까지 대사가 뜨는
  순간의 현재 맵으로 번역을 찾아, 이벤트가 플레이어를 옮긴 뒤 말하는 67행이 제 번역을
  못 찾고 스페인어로 떴다. 2026-08-16에 그 67행을 전역 폴백(맵0)에 사본 37줄로 얹어
  우회해 두었던 것을 걷고, 조회 기준 자체를 이벤트 소속 맵으로 내렸다. 게임 소스 세
  줄이고 전부 살아 있는 인터프리터 안이다. 공통 이벤트는 기준 맵이 현재 맵이라 동작이
  그대로다. 맵0 절은 상류가 넣어 둔 70줄(퀘스트 로그와 공통 이벤트 프롬프트)로 돌아왔고, 67행 전량이 출발 맵에서
  한국어로 조회되는 것을 dat로 확인했다. 곁들여 `build.py`에 `--prune`이 섰다 — 정본에서
  걷어낸 맵 절 키를 dat에서도 떨어낸다. 게임 실행기의 루비로 그 자리를 그대로
  돌려 확인했다 — 지금 소스만 한국어가 나가고, 기준 맵만 되돌린 대조군은 좌표 조회가
  살아 있는데도 스페인어를 낸다(`share/qa-maplookup.rb`). 경위는 [Z-73](tickets/Z-73.md).
- **말투·용어 정본이 stage0로 내려왔고 보호 목록의 재작성 유실을 근치했다(2026-08-18,
  Z-53 줄기 A).** `stage0/voices.yaml`·`terms.yaml`이 직접 편집하는 정본이 되고 옛
  voice-prompts·term-pairs는 은퇴했다(전후 출력 동치 증명). 보호 목록은 마지막 커밋만
  보다 사람 낱건 흔적을 잃던 결함(재생성 시 219페이지 유실 실증)을 누적 규칙으로 고쳐
  유실 0·복원 309페이지가 됐고, 행 단위 출처 목록이 새로 서서 stage0 값에 state·by로
  흡수됐다. CORE_TERMS·UiText 하드코딩 이관, 승인·동결 메타 흡수, 낡음 검사 둘도 함께.
- **번역 조회 밖 텍스트 879자리를 0단계에 등재했다(2026-08-18).** 배틀시설 트레이너
  875명의 미번역이 집계에 처음 잡힌다(수술은 줄기 B 몫). Z-4 전수 판독과 어긋나 있던
  층 6자리(AZ 회상 자막 5 · 맵261 숙박 지문 1)는 층 N으로 바로잡았고, 난천 말투의
  jsonl 등재 누락을 메웠다.
- **맵305 향전시티 대장간 장면의 말투 28행을 유지자 판정대로 고쳤다(2026-08-18).**
  루피코의 격은 존대 그대로 두고 애교 어미를 걷었으며 감탄사 다섯 종을 원문대로 갈랐고,
  올리비에는 격이 뒤집힌 한 줄을 맞췄다. 말투 정본(`stage0/voices.yaml`)과 대장의 루피코
  항목도 그 판정으로 갈았다 — 경위는 [기록](log/2026-08-18-m305-voice-and-review-ui.md).
- **검수 스튜디오가 판정 요청 화면을 겸한다(2026-08-18).** 문안 판정(행)만 받던 화면에
  건 단위 판정을 얹었다 — 브리핑이 맨 위에 서고 승인·기각·보류·답이 `bucket:<id>`로
  쌓인다. 재료는 `stage0/materials.py`가 낸다(자리를 고르는 길 넷: 이벤트·화자·원문·임의
  묶음). 쓰는 법은 [text-pipeline](guides/text-pipeline.md) 「판정 재료와 판정 요청」.

- **층 오배정 165행을 사람 층으로 바로잡았다(2026-08-18, Z-67 닫음).** 전투 호출 70 ·
  기능 포켓몬 NPC 54 · 포켓몬마을 주민 31 · 다친 사냥꾼 10이 지문층에 갇혀 격 검사와
  재번역 처방을 못 받던 것을, 층 판정 도구 수리와 명단·예외 등재로 풀었다. 번역 값
  수정은 0이고 화자 없는 확인창 3행은 반대로 시스템 층으로 내렸다. 곁들여 0단계
  정본(stage0)에 사람 수정이 재생성을 살아남는 overrides 층과 판정 재료 생성 도구
  (`materials.py`)가 섰다.

- **맵 대사 조회가 좌표를 먼저 본다(2026-08-17, Z-73 1단).** 지금까지 같은 스페인어 원문은
  어느 자리에 있든 한 값만 받을 수 있었다. 조회에 (맵, 이벤트, 명령)을 얹어 자리마다 다른
  값을 줄 수 있게 했다 — 없으면 지금까지의 경로(현재 맵 → 맵0 → 원문)로 그대로 떨어지므로
  좌표를 등재한 줄만 새 경로를 탄다. 수술은 게임 소스 여섯 줄이고 전부 `Interpreter` 안이다
  (`Game_Interpreter`는 `$RPGVX`에 대입하는 자리가 없어 죽은 코드임을 실측했다). 열쇠는 맵
  절의 같은 해시에 얹었고 정본은 `translate/ko/00-maps.loc.jsonl`로 따로 선다. 실기 확인
  통과 — 훈련의 집에서 사냥꾼과 치유사가 같은 원문을 쓰던 자리가 갈린다. 치유사 세 줄의
  문안은 승인됐고(2026-08-17 「적절해」), ⚠ 뒤에 얹은 일곱 줄(맵25 마리아노 여섯 ·
  맵155 하나)이 판정 전이다. 경위는 [Z-73](tickets/Z-73.md).

- **갈린 표기를 판정대로 통일하고 Z-70을 닫았다(2026-08-17, 두 차례 520행).** 1차로 코퍼스
  근거가 뚜렷한 117건을 272행에 반영하고, 유지자 판정을 받아 나머지 갈래를 248행에 반영했다 —
  「상태 이상」 26행, 수 구간 물결 7행, 접미사 「-님」 붙여쓰기 90여 행, 고유명·UI 용어는 붙이는
  쪽(「하트게이지」·「마녀집회」·「지하묘지」·「26번도로」·「배틀포인트」류), 보조용언은 띄우는 쪽.
  「그림자 포켓몬」 41행은 근거가 단일 관측이라 두었고, 특성명 「타오르는불꽃」·분류명
  「보물상자」는 등재 표기라 손대지 않았다. 미번역이던 트레이너 카드 한 줄도 함께 고쳤다
  (「Tiempo Jugado :」 → 「플레이 시간:」). 남은 몫은 사각지대 하나(Z-71)다. 말줄임표는 2026-08-09 판정이
  이미 정한 자리라 열지 않는다. 판정은 [품질 대장](ledger/quality.md), 경위는
  [기록](log/research/2026-08-17-notation-unification.md).

- **「본가 자구와 어긋난다」던 76행은 세대 차이였다(2026-08-17, 정본 무수정).**
  절03·06·09·11에서 잡힌 76행을 다시 재니 전부 현행 번역이 코퍼스의 **최신 세대** 자구와
  같았고, 목록의 「본가」 칸이 구세대 값이었다. 어긋남을 낸 것은 세대 채택 규칙이 없던 즉석
  대조 스크립트이고, `canon_sweep.py`는 같은 네 절에서 대상 0행을 낸다. 대신 **줄마다 최신
  세대가 달라 정본 안의 표기가 갈린다**는 것이 드러나 Z-70을 그 판정으로 바꿔 열어 두었다.
  실측은
  [경위](log/research/2026-08-17-corpus-divergence-generational.md), 재발 방지는
  [text-pipeline](guides/text-pipeline.md)의 코퍼스 조회 절에 한 문단으로 얹었다.

- **검증이 못 보던 자리에서 새고 있던 값을 잡았다(2026-08-17, Z-69 닫음).** 검증이 24절 중
  여덟만 절을 지정해 본다는 것을 재다가, 트레이너 클래스 121이 스페인어 `Papa` 그대로인 것을
  찾아 **「파파」**로 옮겼다(몬테산토섬에서 「파파 로토」로 뜬다). 함께 걸린 나머지는 손댈
  자리가 아니었다 — 절14의 `F3`·`AZ`·`EricLostie`는 원문이 맞는 값이고, 게임 원본에만 있던
  절23 키 둘은 옛 스크립트가 남긴 죽은 키다. 검사 게이트를 새로 붙이는 몫은 0단계 정본
  (Z-53)이 답할 설계 항목으로 옮겼고, 본가 자구와 어긋나는 76행은 Z-70으로 열어 두었다.

- **멜리아와 AZ의 격을 유지자 판정으로 통일했다(2026-08-17, 28줄).** 스튜디오 메모 둘이
  연 자리다. 멜리아가 아버지에게 쓰던 해요체 열두 줄을 반말로 맞췄고(원문은 스무 자리
  넘게 전부 tú), 최종병기 발사 뒤의 맵501(장면 1)은 장면 안에서 격이 넘어가게 두었다 —
  따지는 앞부분은 반말이고 작별을 고하는 대목부터 존대다. AZ가 알카에게 쓰는 말은 하오체로 통일했다 — 원문에서 AZ만 usted를 쓰고 알카는
  tú로 명령하는 비대칭이 근거이고, 맵496(장면 3)에서 등을 돌린 뒤는 하대 그대로다.
  격 검사기 산출은 손대기 전과 같은 133곳이고, 청자가 어른인 두 줄과 주인공에게 하는
  하게체는 손대지 않았다. 판정은 [voices 대장](ledger/voices.md)의 두 절.

- **번역표가 닿지 않던 이름들을 잡았다(2026-08-17, Z-63 닫음).** 포켓몬 이름 넷은 값이 코드·
  데이터에 리터럴로 박혀 어느 절에도 담기지 않던 자리라 소스 수술로 갔다 — 간수 피노의 대포무노
  별명(폴리) · 프레스코 풍차의 보스(수호장치) · 최종병기의 분신(꽃, 한 마리에 두 번 대입돼
  두 자리를 함께). 이어서 이벤트가 만드는 트레이너 이름을 전수로 세어, 맵 314(칼로스 동부 전투)의
  까메오 169명을 절14에 음역해 얹었다. 배틀 시설 명단 875개는 제작자 지인·커뮤니티 핸들이라
  **옮기지 않기로 판정**했고(옛 배틀타워 이벤트 269개는 도달 불가로 확인), `Becuille`은
  「베 힐러」에서 「베퀼레」로 뒤집었다. PBS는 손대지 않는다는 것이 조사의 소득 하나다 —
  고치는 순간 재컴파일의 방아쇠가 되어 `Data/`의 dat 21개가 원본에서 다시 구워진다.

- **전투 종료 대사 358좌표를 도구 없이 전수로 읽고 59줄을 고쳤다(2026-08-17).** 유지자 지시로
  격 검사기를 한 번도 안 돌리고 이벤트 페이지를 읽어 판정했다. 이 대사들의 화자 288명은 게임
  안에서 말하는 곳이 그 한 줄뿐이라 「평소 급」이 성립하지 않아 **검사기를 고쳐도 못 보는
  자리**였고, 대신 같은 페이지의 도전 대사를 기준으로 삼았다. 살아남은 갈래는 호칭이 갈리는
  자리(「그대」로 일관하는 페이지에 「너」 한 줄) · 급이 뒤집히는 자리 · 대장이 어미까지 등재한
  자리다. **어미의 결이 다른 것은 어긋남이 아니라는 판정**으로 열 자리가 철회됐고, 그 원칙을
  지침에 절로 올렸다. 유지자 판정 넷이 대장에 올라갔다 — 어둠의 탑 인물은 말투를 손대지 않음 ·
  난천은 본가 인물 말투 등재 · 집단 화자 표는 「무엇으로 그 집단을 가리는지」를 함께 적어야 함 ·
  아조스단 신병의 전투 대사는 조무래기 구어. 곁들여 스페인어 여는 느낌표 여덟 줄을 지웠고,
  층이 지문으로 잡힌 전투 대사 70행은 Z-67로 열었다. 조사 기록:
  [전수 훑기](log/research/2026-08-17-battle-line-register-sweep.md) ·
  [직함 재료 104좌표](log/research/2026-08-17-battle-register-class-materials.md).

- **Z-61을 닫았다(2026-08-16) — 전이 뒤 대사가 원문으로 뜨던 자리.** 게임이 대사를
  현재 맵 → **맵0 전역 폴백** → 원문 순으로 조회한다는 것을 스크립트에서 읽어, 도착 맵마다
  줄을 얹는 대신 맵0에 **37줄**을 얹었다. 유지자 제보 넷이 여기 속한다. `build.py`가 맵 절에도
  추가분을 받도록 넓혔고(정본 `translate/ko/00-maps.add.jsonl`), 포터블 루비로 게임의 조회
  함수를 그대로 돌려 시험판에서만 한글이 나오는 것을 대조군과 함께 확인했다. 실기 화면
  확인은 지나온 자리라 못 했다. 경위는 [기록](log/2026-08-16-Z-61-closed.md).
- **도구 둘의 시야를 고쳤다(2026-08-17).** `register.py scan`이 출력 경로를
  `docs/log/research/2026-08-06-register-mismatch.md`로 상수로 잡고 있어 돌릴 때마다 그
  날짜의 박제를 덮었다 — 기본을 실행일 파일로 바꾸고 경로를 인자로 받게 했다(오늘 판:
  [2026-08-17](log/research/2026-08-17-register-mismatch.md)). `verify.py`의
  UI 치환표 오폭 검사는 문자열 쌍 30개만 세고 인명 정규식 쌍 23개를 통째로 빠뜨리고
  있었다 — 53쌍 전부를 보게 넓혔고 오폭 후보는 그대로 0이다.
- **Z-62를 닫았다(2026-08-17) — 배지 이름이 스페인어로 뜨던 자리.** 그리기 함수를 후킹하는
  길은 글꼴 모드가 같은 메서드를 재정의해 **모드 설치 순서에 따라 조용히 죽는다**(작업 중
  실제로 순서가 뒤집혔다). 그래서 `UI Text KR`이 로드 시점에 `FANCY_BADGE_NAMES`의 원소를
  치환표에 통과시키는 길로 갔다 — 이름을 다시 적지 않으니 값은 한 곳에만 살고, 상수를
  정의하는 게임 본체 절이 항상 먼저 실려 순서와 무관하다. 포터블 루비로 그 두 절을 잘라
  돌려 **열두 개 전부 한글이 되는 것**을 확인했다(화면 렌더링은 미확인). 경위는
  [닫음 기록](log/2026-08-17-Z-62-closed.md).
- **Z-60을 닫았다(2026-08-17) — 전투 종료 대사 357행이 도구 시야에 들었다.** 대사가
  말풍선 명령이 아니라 `pbTrainerBattle`의 셋째 인자에 있어 귀속표가 못 보던 자리다.
  `speaker.py`가 스크립트 명령도 긁고(인자 파서로 — 화자와 대사가 같은 호출 안에 있다),
  `register.py`가 그 행을 검사 대상에 넣되 평소 급 계산에는 안 넣는다. **기존 19,310행은
  일곱 칸 전수 대조로 무변화**를 확인했고, 새 행은 `kind="battle"`이라 유료 재번역
  배치에 안 딸려 든다. 스튜디오 조인표도 화자 칸을 귀속표의 한국어 직함+이름으로 채워
  재생성했다(빈 칸 365 → 8). 사람이 볼 목록은
  [전투 대사 격](log/research/2026-08-16-battle-line-register.md), 경위는
  [닫음 기록](log/2026-08-17-Z-60-closed.md).
- **동결 목록이 제 파일로 섰다** — 재번역 동결이 빌드 추가분 파일에 얹혀 살던 것을
  `translate/data/frozen-keys.jsonl`로 옮기고, 역할이 끝난 `23-script-texts.add.jsonl`을
  지웠다. 절23 재번역 대상은 2,313행 그대로다.

- **Z-65를 닫았다(2026-08-16) — 한 NPC 안에서 격이 널뛰던 자리 정리.** 유지자가 스튜디오
  메모로 「배틀 전-후 격맞춤 안 된 것 너무 많음」을 찍은 데서 출발했다. 이벤트 하나가 곧
  NPC 하나임을 좌표 실측으로 확정하고(같은 칸에 겹쳐 서는 이벤트 0건), 원시 182이벤트를
  거짓 양성 다섯 꼴(말줄임 종결·명사 나열·인용·괄호 지문·**혼잣말**)과 청자 전환으로 걸러
  배틀 20 · 비배틀 45 · 컷신 65로 갈랐다. **정본 78줄**을 고쳤다.
  곁들여 나온 것 셋: `register.py`의 이중 말투 제외가 직함형(`Alcaide Pinot`·`Capitán
  Merlot` 등)을 못 잡아 160행이 새던 것을 고쳐 어긋남 목록이 326→145로 떨어졌고,
  인물로 잡혔으나 인물이 아닌 20줄을 Z-55에 얹었으며, 로토 선장의 말투 등재가 틀린 것을
  바로잡았다(해적은 그의 부하다). 조사 기록:
  [배틀 20](log/research/2026-08-16-battle-register-20.md) ·
  [비배틀 45](log/research/2026-08-16-register-nonbattle-45.md) ·
  [컷신·전체 65](log/research/2026-08-16-register-mixed-events.md) ·
  [감옥 간수](log/research/2026-08-16-prison-guards.md) ·
  [교환 정형구](log/research/2026-08-16-trade-formula.md).

- 씬 판별을 재고 Z-57·Z-59를 닫았다(2026-08-14) — 블라인드 라벨을 700건까지 쌓아
  (라운드 3이 오염 없는 첫 측정) 정밀도 0.89~0.94 · 재현율 1.00 · 무작위 표본에서 놓친
  메인스토리 0을 얻었다. **그런데 이 구분이 번역 처방을 안 바꾼다**는 것이 더 큰 결과이고,
  거기서 구분을 세우는 순서를 원칙으로 못 박았다(quality 대장 「구분은 경험에서 처방으로,
  처방에서 신호로 유도한다」). 곁들여 **기능 창구 357페이지**를 이벤트 명령의 기능 호출로
  갈라 `translate/data/scene/counter-pages.jsonl`에 담았다 — 정본 982줄이 처방 등급 2로
  확정된다. 닫음 기록: [Z-57·Z-59](log/2026-08-14-Z-57-Z-59-closed.md).
- 장면 판정 기반 정비(2026-08-14) — 진행 플래그 등재의 값을 셋에서 **여섯**으로 넓히고
  (메인스토리 · 서브퀘스트 · 상태 플래그 · 임시 플래그 · 모드 · 모름) 잘못 꽂혀 있던 것을
  옮겼다: 서브퀘스트 49종(게임 안 의뢰 게시판이 근거) · 모드 4종(그중 셋은 등재에 아예
  없었다) · 임시 플래그 11종 · 107 PokeFlauta 복귀. 층 판정 결함 셋도 고쳤다 — 대문자
  이름(F3·AZ)이 인물 목록에 못 들어가던 것, 어간이 `f3ow`를 답 너머까지 깎던 것,
  배제 명단이 둘이던 것. 귀속표 235행의 층이 바뀌었고 번역값은 안 건드렸다.
  변수 축 등재 191종이 새로 생겼고(스위치 축의 짝), 블라인드 라벨 300건이 **이 프로젝트의
  첫 메인스토리 정답표**다. 채점 도구는 `translate/scene_eval.py`.
  판정: [quality 대장](ledger/quality.md) 「장면 판정 값은 여섯」 ·
  [voices 대장](ledger/voices.md) 「집단 화자는 정본 인물이 아니다」.
  재현율은 아직 안 재였다 — Z-57.

- 스튜디오 메모·제보 라운드(2026-08-13) — 가방 프롬프트 본가 정합(선택했다→어떻게
  할까?, 등록 해제 「리셋」→「해제」) · 미라 맵79 존대 오역 3행 하대 복원(보호 확산이
  재번역을 가린 결함은 Z-54 4항) · 덴드로 존대 통일(아티 가설 기각, voices.md 판정
  뒤집음) · 프로파노 gitana 마지막 줄 순화 · 창작 기술 Tromba Marina 바다회오리→용오름
  (본가 Whirlpool과 이름 충돌 해소, 유저 제보) · 장난 제보 1건 옐로카드. gitana 두 결
  판정은 등재됐고 7문구 재적용은 Z-56. 이름표 없는 NPC 대사 정비 완주. 마지막 갈래(컷신·대화 재번역): 풀 정제
  (지문·시스템 98페이지 제외, 204p/1,540행→106p/1,037행) → 전량 배치 31p/266행
  ($0.199, 반려 0) → 스튜디오 판정·반영(정본 82행+통일 전파 14종+표기·화석 정합) →
  열쇠 충돌 해소(판별식 수선·갈래 판정 16건·맵282 반말 통일·격 일관성 쓸기).
  잔여 미상·지문 610행은 Z-44, 재번역 본대는 Z-47.
  경위: [닫음 기록](log/2026-08-13-Z-4-closed.md) ·
  [사고 기록](log/research/2026-08-13-unified-persona-incident.md).
- Z-46 닫음 — 층 배정 결함 둘 수선(voices.md 60명 명단이 행수 문턱보다 우선, 어간
  충돌 flare·flara·luigi 전수 검사)과 cls·once·flags 세 칸 배선, 귀속표 재산출. 층
  수치 정본은 커밋된 표 기준 PS 682p/8,483행 · PC 2,218p/5,389행 · N 1,802p/3,209행
  (옛 박제 수치는 재현 불가 폐기 — [규명](log/research/2026-08-13-n-layer-delta.md)).
  판정: luigi=PC, batch_npc는 Z-47에서 은퇴. 경위는 [닫음 기록](log/2026-08-13-Z-46-closed.md).
- Z-47 닫음 — 잡담 풀 보충 + 재번역 라운드 완주. 보충 산정(2,404행)·페르소나 39종
  등재 → 전량 배치 2,221행($0.905) → 3층 선별·선판독·스튜디오 검수 → 정본 584행
  반영 → 격 쓸기·유실 감사(덮인 사람 값 38행 복원, 보호 108이벤트 소급) → 되돌림
  460행 점검(시스템 문구 52건 수선, 고고트 훼손 45줄 복구). 구조 수선: SoT 이관·
  프롬프트 결함 4종·요청 묶음·선별 필터·스튜디오 --no-skip. 잔여는 Z-54(파이프라인
  안전판)·Z-55(층 오배정). 경위: [닫음 기록](log/2026-08-13-Z-47-closed.md).
- Z-48 닫음 — 디버그 모드를 하나로. 구판 둘(`디버그 모드`·`Debug Toggle`)을 지우고
  P키 토글 한 모드로 합쳤다. 키 감시는 `Scene_Map#update` 별칭이 아니라
  `Events.onMapUpdate`에, 키 판정은 Win32API가 아니라 `Input.triggerex?`에 건다 —
  mkxp-z 대체층에 없는 함수가 비Windows에서 조용히 0을 돌려주던 경로를 없앴다.
  패드로는 LB+RB로 켜고 끄며, 디버그 메뉴(원작 F9)는 RB 단독으로 연다. 실기 확인 완료.

- Z-49 닫음 — Controller UX 엑스박스판(모드 판 1.0). **배선 기준을 실측으로 다시
  잡았다** — 실행기 기본 배치가 이미 아래 버튼 확인·오른쪽 버튼 취소여서 맞바꿈이
  필요 없었다(경위: [2026-08-13 조사](log/research/2026-08-13-pad-default-bindings.md)).
  패드로 못 닿던 자리를 메웠다: 등록 도구와 도감 검색을 X로, 도감 쪽 넘김을 LB·RB로,
  가방 검색창을 확인·취소 버튼으로 닫기. `Scene_Map#update` 재정의를 걷어내고 라벨은
  일시정지 메뉴·연금술 화면까지 패드 표기로 덮는다. 가방 그림 16장과 도감 배경 둘은
  원본 글꼴·팔레트를 그림에서 떠 와 키 표기만 갈아 끼웠다. 실기 확인 완료.
  ⚠ 이 실행기는 패드 연결 여부를 알려 주지 못한다(`Input.joystick`이 패드를 꽂고
  버튼을 누른 채로도 nil) — 「패드 없으면 비활성」은 못 만들어 접었다.

- Z-51 닫음 — 글꼴 모드가 코어 수술 없이도 선다. 굵은 한글 바이트 판독에서 `getbyte`를
  버리고 `unpack("C")[0]`만 쓴다. 원작 `Map - Klein`이 조건 없이 `alias getbyte []`를
  걸어 신형 루비에서도 정수 대신 문자열이 오던 것이 튕김의 뿌리였고(경위:
  [2026-08-13 조사](log/research/2026-08-13-getbyte-klein-alias.md)), 지금까지 우리를
  살린 것은 모드가 아니라 코어의 별칭 가드였다 — 모드만 따로 받으면 모든 글자에서
  죽는 경로였다. 구판 실기(mkxp-z 1.8.7)에서 옛 코드와 값이 같음을 확인하고, 정본
  한 자리를 고쳐 글꼴 셋(DPPt·갈무리·BW)을 재조립했다. 코어의 가드는 그대로 둔다.

- Z-43 닫음 — 갈림 허용 목록의 무표시 잔류 43그룹 재판정 완주: 전판 통일 6 + 정렬 5 +
  반말끼리 통일 13(유지자 판정) + 격·결 갈림 유지 19, 정본 26행. 갈림 허용 목록 56→37건,
  남은 갈래는 전부 사유가 실체(화자 격·결)로 등재됨. 판정표는
  [2026-08-13 박제](log/research/2026-08-13-z43-verdicts.md).
- 통일 원문 관리층 — 여러 맵 복제 원문의 번역을 목록 둘로 자체 저장: 통일
  `unified-phrases.jsonl`(391건, 원문→값)과 갈림 `divergence-allowed.jsonl`(56그룹
  160갈래 — 갈래별 값·맵·스프라이트). unified.py check/restore/sync, verify가 두 목록
  값 대조·미등재 갈림을 FAIL로 차단, 배치는 통일=전판 1회·갈림=(원문,화자) 1회만
  번역하고 반영이 전 자리에 전파. 큰 뭉치 13그룹 재판정 126행(기술 떠올리기
  스프라이트별 판·치유사 전판 통일·교환 정형구 격 판) + 말줄임 갈림 2행 수렴.
  경위·원칙은 quality.md 「통일 원문은 하나로 관리한다」. Z-43(무표시 잔류 43건) 신설.
- Z-5 닫음 — 말투 지시문이 빈 조연의 말투 등재 완주. 사정권 재실측 41화자/143행
  (티켓의 70명 중 29명은 8/6 검수 승인·페르소나표 갱신으로 이미 사정권 밖) 전수
  판독으로 개성 13인 인물 표 등재 + 잔부 27태그 기본값 흡수, 배치 빈 말투 화자 0명
  확인. 갈림 4곳은 실측 후 판정 — 마부 하대 통일 2행 · 브리오프(=유죄 판결을 받은
  남성, 동일 인물 발견) 하게체 7행 · 피스티아 해요체 5행 · 세레나 반말 1행 ·
  꼬마마녀는 유지자 재판정으로 **어른인 척하는 노파투**를 채택해 세 장면 7행 통일. 표기는 Volus→「월로」(본가 LA 공식명, 코퍼스
  실측) 치환 5행 + 조연 이름 11종 canon 등재. 말투표 키 불일치(「여성」)·아우레
  중복 항목 해소.
- 새 모드 `Battle Order` — 배틀 처리 순서를 본가 8·9세대 기준으로 맞춘다(Z-41 닫음).
  행동이 하나 끝날 때마다 남은 순서를 다시 계산해서, 턴 도중 걸린 마비와 교체로 들어온
  포켓몬의 등장 특성이 깐 트릭룸·날씨가 그 턴에 반영된다. 라운드 끝 지속 데미지
  열한 자리와 먹밥 계열 세 자리에서 연출·메시지를 체력 변화 앞으로 옮겼고, 선제의발톱
  메시지를 그 포켓몬이 움직이기 직전으로 옮겼다(판정 시점은 그대로라 순서는 안 바뀐다).
  원본 메서드 넷을 통째로 다시 정의하는 모드다 — 무엇을 어디서 떠 왔는지는 그 폴더의
  `README.md`, 근거는 [배틀 순서 감사](log/research/2026-08-10-battle-order-audit.md).
  부팅은 두 루비에서 확인했고(닫을 때의 상태는
  [Z-41 기록](log/2026-08-10-Z-41-closed.md)), **실기도 유지자가 확인해 정상 동작**
  (2026-08-17). 합본에 실을지는 다음 합본에서 판정.
- 트릭룸 문구 둘을 본가 자구로 — 자연 종료 문구를 「뒤틀린 시공이 원래대로 되돌아왔다!」로,
  「차원을 뒤틀었다」로 남아 있던 옛 판을 「시공을 뒤틀었다」로(그 키는 지금 게임 코드에
  없어 안 돈다). 설치와 해제가 같은 원문 하나를 써서 번역표로는 못 가르는 문제는 판정으로
  그대로 두었다 — quality 「트릭룸 설치·해제 문구는 안 가른다」 절.
- 같은 원문 딴 판 통일(Z-28 닫음, 741행 + 우두머리 계열 62행) — 갈린 원문 283개를
  시트 두 판으로 결산해 A절 195그룹(시스템 지문·화자 단일) 전량과 B절(화자 갈림 86)
  중 지시 30그룹을 통일. 너즐록 실패문 26판→1판. 화자 없는 확인창은 본가식 평문
  「~할까?」, 결과 지문은 평서, 아이템명은 07-items 정본. 「알파」 계열은 본가(LA)
  명칭 「우두머리」로(색상 채널 라벨 「알파:」는 유지). 남은 갈림 58개는 전부 의도
  (화자별 유지 56 + 유지 판정 1 + 손수정 1). 시트는
  [1부](log/research/2026-08-10-z28-sheet1.md)·[2부](log/research/2026-08-10-z28-sheet2.md).
- 로시욘 말투 통일(34행) — 유지자가 스튜디오에서 첫 등장 장면(맵58 왕들의 성소,
  같은 이벤트 네 벌 28행)을 하오체로 갈아입힌 데 맞춰, 합쇼 존대로 남아 있던 맵36
  로시욘 저택 파티 6행을 같은 격으로 옮겼다. 함께 새어든 표기 흔들림 다섯 자리
  (「로시용」 4행 · 「유화시티」 1행)를 용어 대장 표기로 회수했다. 판정은 voices
  로시욘 항목에 옛 판정(사교장 합쇼체)과 함께 등재.
- 유지자 스튜디오 손검수 묶음 — 탈것 이름을 「고고트라이드」로 통일(「포켓라이더
  고고트」·「라이드고고트」·「고고트 라이드」가 섞여 있던 40여 자리), 포켓몬 교체
  확인 문구에서 이름 보간 `{1}` 제거(절23 두 줄), 「멜랑콜리」를 「쓸쓸함」·「애수」로,
  옛 왕들의 별칭 넷과 잡담 여러 자리 손질.
- 박스·PC 화면 UI 정리(95행) — 스튜디오 미결 메모 「배치한다」에서 출발해 박스·도구
  보관·편지·정화의 방을 통째로 맞췄다. `Place`·`Colocar`를 「이동한다」로, `Dejar`를
  「맡긴다」로 고치고, 영어 키 벌에 남아 있던 미번역을 스페인어 짝에서 채웠으며, 벽지
  이름과 PC 메뉴 라벨을 한 벌로 통일했다. 조작 설정 화면에서 키 이름이 겹쳐 `P`·`G`
  키를 고를 수 없던 것도 원문 복원으로 풀었다. 근거는 quality 「박스·PC 화면은
  스페인어 키 벌이 정본이다」·「번역이 해시 키로 쓰이는 자리는 원문 그대로 둔다」 절.
- 망각의 감옥 죄수 대사 정리(27행) — 원문에 근거 없는 문두 「이봐」 걷어내기(정본
  45→27개), 감옥 맵을 재사용한 맵249와 갈려 있던 아홉 자리 통일, 블랙잭 상대 죄수가
  한 이벤트 안에서 오르내리던 하오체를 반말로, 괄호 지문 셋을 서술형으로, 블랙잭의
  `carta`를 「편지」로 옮긴 오역 수정, 카탈루냐어 두 줄은 원문 노출. 근거는 quality
  「원문에 없는 문두 감탄사는 넣지 않는다」 절과 glossary 「카탈루냐어 대사 = 원문
  노출」 절. 유지자 스튜디오 손검수가 같은 구역에 함께 실렸다.
- 잡담 말투 선제 교정 1차 — 유지자 제보 1,052행의 수정 축 분석에서 뽑은 방향을
  채점된 잡담 게이트(정밀도 90.9%) 위에서 적용. 4방향 43줄(점술가 반말, 카페 총사
  본가 표준 반말, 신사, 늪 주술사 어른말). 근거는
  [수정 축 분석](log/research/2026-08-09-suggestion-axes.md)과
  [채점판](log/research/2026-08-09-scene-scorer.md).
- 잡담 게이트 애매 층 트리아지 완료(Z-40 닫음) — 1,478페이지를 flash로 분류해
  제외 1,286 확정·통과 192(Z-4 배치 선별 신호로 인계), 말투 2차 확장분은 0건으로
  확정. 1차 보류 2건 반영: 카페 총사 교환 첫 줄 반말 하향(2줄), revolucionario
  평시 3줄을 평범한 반말로(흉흉한 시기 2줄은 유지 — 유지자 규칙).
  [결산](log/research/2026-08-09-ambiguous-triage.md).
- 잡담 급 통일 배치 완료(Z-4 1갈래 닫음) — 말투 혼재 43종을 시트 두 판으로 결산해
  84줄 반영(유지자 줄 단위 판정 + 메인 재작성): 카페 교환 존대·하게 분화, 병사 구출
  3줄 지문화, mosqueteraMontada 총사 계열 하향 14줄, 전투 우위·의식·낙서 문구 통일,
  PB→BP 5줄. 분류기를 B1~B7로 확장(하게·대화단정 분리, 호칭 「군?」·느낌표 명령
  수리 — selftest 17건), 페르소나표 74행 현행화. 지침 급 표도 갱신.
  이어서 결함 패턴 스윕도 결산(Z-4 2갈래 닫음) — 울음소리 17줄 종명 기반 재생성
  (캥카·슬리프·지가르데 「지가아아아아!」 5곳 통일·이벨타르/제르네아스 본가 울음
  「키야아아아아!」, 뱀파이어 WRY는 원문 보존 판정), 「당신」은 105행 검토 후
  지문·안내문 15줄만 손질(서비스 존대·기도문은 유지), 문두 감탄사 후보 74행은
  전수 판독 결과 결함 0건(전부 원문 대응·지시어·점술사 버릇·오탐), 페이크몬 옛 이름
  잔존 0건 확정.
- 이탤릭 로마자 잔존 259건 전량 판정 — 11자리 옮김(슈발리에 음차·곡예단 등),
  나머지는 의도된 외국어(프랑스 감탄사·가라르 영어·러시아어 장치)로 남김 확정.
  [판정 재료·결산](log/research/2026-08-09-italics-verdict-materials.md).
- 정본에 새어든 제보 붙여넣기 꼬리 개행 4자리 제거(절23의 22자리는 게임이 기대하는
  형식이라 유지 — [조사](log/research/2026-08-09-crlf-sect23.md)).
- 신형 루비 실행기에서 죽던 열 자리 수술(Z-34 시험대) — 정규식의 높은 바이트 표기 ·
  바이트를 문자열 첨자로 읽는 자리 · `LargePlane`의 부모 초기화 · `Errno::EISDIR` ·
  `nil.id` 전제 · `String#getbyte` 무조건 별칭 · `StringInput#getc` · `proc` 안의
  `return` 163곳 · 이름 입력창의 키 번호와 문자 입력 모드. 데스크탑 mkxp-z 신판
  시험대에서 잡아 원판(루비 1.8.7)에서도 회귀 확인.
  [시험대 기록](log/research/2026-08-09-mkxpz-bench.md).
- 합본 v5.3 조립 준비 — Type Matchup을 세 갈래에 기본 동봉, 디버그 추가판은 켜진 채로
  나가고 Debug Toggle이 그 위에 얹힌다. 모드 이름 끝의 `Z`를 뗐다(`Type Matchup`·
  `Debug Toggle`). **배포는 번역 작업을 더 한 뒤로 미룸**(유지자 판단).
- 모드 `Native Tilemap`(맵 렌더러를 엔진 내장으로 — Joiplay 렌더 문제 우회). 유지자
  Joiplay 실기에서 약한 개선 확인, 합본에는 안 싣는 선택 모드로 확정(Z-35 닫음).
- 도구·프롬프트: 용어 잔존 전 절 검사기 `translate/termcheck.py`(깨진 말더듬 검출
  포함, names-terms 절차 등재) · 재번역 프롬프트 Naturalness rules J~Q(직역투 해소
  8규칙, 코퍼스 근거).
- 상태이상 Caduco 표기 재판정 — 쇠락 → 쇠약. 정본 27건 치환(형용사 「쇠락한」 2건은
  유지), 용어 자료·share 문서 동반 갱신, 상태 아이콘 두 그림(statuses ·
  battleStatuses)에 「약」 자형을 새로 그려 게임 설치본과 Z-GUI 모드에 반영.
  근거는 glossary 「estado Caduco」 행.
- 창작종(페이크몬) 한칭 15종 재작명(Z-37 닫음) — 디시 유저 배포판의 재작명을 검토·회수,
  판정 기준은 진화 계보의 공식 한칭 말줄기 유지(슬리프→슬리퍼→슬리피 등). 프레야라는
  유저안 수정 채택, 아우레토스크·킬기시아는 제외. 스프라이트·게임 데이터는 편입 없음.
  근거는 glossary 「창작종(페이크몬) 한칭 15종」 절, 경위는
  [Z-37 결산](log/2026-08-09-Z-37-closed.md).
- 로컬 스튜디오 완성(Z-22 닫음) — `translate/fixgui.py`가 정본을 직접 고치는 편집기가
  됐다. 일괄 바꾸기 세 갈래(미리보기·선택 적용, 원문 조건에 걸려 빠진 행은 목록으로) ·
  동작 묶음 이력과 묶음째 되돌리기(뒤에 다시 고쳐진 행은 건너뜀) · 카드의 맵 칩과
  이벤트 칩(그 대사가 속한 이벤트-페이지를 명령 순서대로, 같은 원문이 선 다른 맵도).
  화면 코드는 `translate/fixgui.html`로 분리, 규칙 검증은 `translate/test_fixgui.py`.
  이로써 웹 스튜디오로 dat를 고쳐 `harvest.py`로 회수하던 우회가 유지자 쪽에서 사라진다
  (웹은 유저용으로 유지). 화면 규약은 text-pipeline 「로컬 스튜디오」 절.
- 이벤트 중복 구조 실측 — 한 맵의 (맵, 원문) 묶음 13,417 중 2,606(19.4%)이 자리
  둘 이상이고 갈래가 셋(다른 이벤트 1,877 · 같은 이벤트 다른 페이지 474 · 한 페이지 안
  반복 255)이라는 것을 귀속표 19,310행으로 세어 events-and-speech에 등재했다.
- 포켓몬센터 회복 대사·폼체인지 용어(Z-33 닫음) — 같은 원문이 맵마다 다르게 번역돼
  있던 회복 흐름 네 슬롯을 각각 한 문장으로 통일(113행). 대기는 본가 자구
  「잠시만 기다려 주세요」로 방향을 뒤집었고, 완료는 본가 완료·배웅 자구를 이었다.
  덤으로 「변신」을 쓰던 호텔 폼체인지 NPC 다섯 줄을 본가 용어에 맞췄다.
  자리별 전문은 [흐름 표](log/research/2026-08-09-z33-pokecenter-flows.md), 판정은 quality 대장.
- 어버이(OT) 성별 표시(Z-39 닫음) — 번역표 값 둘(수컷/암컷 → 남자/여자)과 스크립트
  수술 한 줄(박스 표시의 공용 키를 어버이 전용 키로 교체). 판정은 quality 대장.
- 커뮤니티 모드 둘 이식(Z-36 닫음) — `Type Matchup Z`(기술 선택창 상성 색칠) ·
  `Debug Toggle Z`(W키 `$DEBUG` 토글 + 디버그 중 전투 후 회복). 디시 배포판에서
  섹션만 떼어 우리 모드 형식으로 옮겼고 실기에서 둘 다 동작 확인(유지자, 2026-08-09).
  게임에는 modkit으로 설치돼 있다 — 배포에 실을지는 [Z-34](ROADMAP.md) 몫이다.
- 소소 묶음 결산(Z-30 닫음) — 유지자 제보 12건 반영 · 맵127:25 조사 통일 ·
  너즐록 「영웅」 표기 수술(치환표) · 인트로 모드 선택 PNG 「노말 모드」→「일반 모드」
  재렌더 · 위치표 시트 영어 잔존 62셀 해소(판정된 미확인 7종만 유지). 경위는
  [Z-30 박제](log/2026-08-09-Z-30-closed.md), 표기 판정은 glossary.
- 절23 UI 문구 본가 자구 정렬 완결(Z-3 닫음) — 잔여 264행을 전수 분류해 유지자
  판정으로 결산했다. 반영: 메뉴 동사형 49 · 저장→리포트 전 절 41 · 선택지 네→예
  264(+전역 확인창 Si) · 벽지 8 · 주머니(배틀·나무열매) · 포켓몬 도감 · 전당등록 ·
  풀베기·키우미집 · 화자 실측 판정 5(점원 존대 등) · 독립 말줄임표 여섯 점 79.
  오탐 130여 행과 죽은 키는 스크립트 실측으로 갈라 유지. 행 단위 근거는
  [분류 기록](log/research/2026-08-09-z3-triage.md), 판정 요지는 quality 대장.
- Essentials 번역층 경로 조사(Z-38 닫음) — 킷 판본 v16.2 실측, 한국어 완성 번역층은
  웹 미발견. 결론은 「다음 게임의 한국어 층은 이 저장소의 절23 정본」 — 이식 순서를
  reuse-playbook 「시스템 문구는 이식부터」 절에 등재. 조사 전문은
  [기록](log/research/2026-08-09-essentials-v16-layer.md).
- 본가 자구 정렬 마무리(Z-2 닫음) — 도감 분류 61행을 종번호 대조표
  `translate/canon/genera.jsonl`(PokeAPI 산, 공식 덤프 zkn_type 교차 검증)로 갈고,
  verify에 분류 상시 검사를 붙였다(일치 898 · 불일치 0). 트레이너 클래스는 유지자
  판정으로 17행(셰프·포켓몬 트레이너·메르헨소녀·이상한 자매·레인저스, 대장 유지),
  올리비에 호칭을 「교수」에서 「박사」로 91행 통일(말더듬 꼴 포함, 근거는
  glossary·quality 대장). 이름 절 잔여 34행은 전량 오탐/원문 결함으로 유지 판정.
- 본가 자구 정렬 1차(Z-2·Z-3 진행) — 설명문 절 셋 590행(기술 415 · 도구 168 · 특성 7)과
  도감 설명 103행, 절23 UI 문구 147행(배틀 표준 문구·리본 설명·트레이너 메모·타입명,
  시합 룰 Set→토너먼트 실오역 포함)을 코퍼스 완전 일치로 갈아 끼웠다. 대조 도구는
  `translate/canon_sweep.py`로 상비화. 잔여는 로드맵 Z-2·Z-3.
- 번역표에 키가 없어 스페인어가 그대로 뜨던 스크립트 문구를 전량 들였다(Z-1 닫음) —
  잔여 30종(랜더마이저 메뉴 29 · 전화 말줄임 1)을 번역해 재추출 갭 0을 확인. 연구
  당시 102종 중 68종은 앞선 세션들이 처리했고, 4종은 추출기의 루비 escape 오독이
  만든 허수였다([재추출 기록](log/2026-08-09-Z-1-closed.md)).

- 유지자 제보 반영 넉 장 — 173건(08-07) · 137건+dat 손수정 24건 · 114건 · 47건(08-08),
  이어서 스튜디오 회수 39자리 · 아우레 회상 존대 통일 20행 · 개별 격 판정 9자리(08-08).
  용어 일괄(쇼다운 내보내기·섭정 레벨·기술 떠올리기 등)과 궁전 구간 말투 손질이 큰 몫.
- 교환 NPC 정형 문구를 맵 격에 맞춰 나눠 썼다(Z-27, 88줄) — 본가 자구 「귀여워해 줘」·「선두」
  채택, 격이 섞인 카페 여덟 맵은 다수결 격으로 페이지째 통일.
- 귀족 스프라이트 NPC의 격 혼용을 정리했다(Z-25, 6줄) — 청자가 플레이어면 존대, 적대
  대치는 하대, 안내는 끝까지 같은 격. 화가 지망생 스프라이트를 페르소나표에 올렸다.
- 아우레의 장면별 격을 전수로 세웠다(Z-26, 4줄) — 가면은 존대, 정체를 드러낸 뒤 인간에겐
  하대, 더 강한 상대 앞에선 굴복 존대. 「귀사」 오역(compañía=일행)도 함께 고쳤다.
- 유저 제보 판정 122건 결산(Z-6 닫음, [결산 기록](log/2026-08-09-Z-6-closed.md)) —
  맵112 로시욘 저택·맵108·맵120 등 말투 아홉 절과 단건들, 도구 획득 문구를 본가 자구
  「손에 넣었다」로(숨은 도구만 「발견했다」), 「초진화의휘석」·「트러플」 용어 통일,
  볼프람은 자동인형에게 하대(대장에 예외 등재).
- 새 제보 45건 반영 — 「나무열매 관목이다!」 26개 맵 통일, 맵17 스타터 선택지를
  「풀타입/불타입/물타입」으로, 맵108 크리산토 존대.
- 도감 검색 오역 셋(DEF·악·페어리) · 말더듬 표기 셋 · 레스토랑 메뉴 통일.
- 출혈 상태 문구가 상태이상을 말하게(화상·마비와 같은 꼴, 네 줄) · 튀어오르기 설명을
  본가 자구로.
- 굵은 한글 그리기가 모바일 실행기(Runa·RPG Player)에서 튕기던 것을 고쳤다(Z-31) —
  `getbyte` 없는 1.9계 실행기에서 String 대 정수 비교가 되던 자리를 `unpack`으로.
  코어를 다시 만들기 전엔 유저에게 안 닿는다.
- 코어에 신형 루비 호환 층을 넣었다(Z-31, 구 Z-32) — 맨 앞 심 섹션(없는 API만 채움:
  `nitems`·`Thread.critical`·`File.exists?`·`Fixnum`·`getbyte`)과 1.8 전용 구문
  수술(`when N:` 콜론 23곳·rescue 밖 `retry` 1곳). 모바일(mkxp-z, 루비 3.1+)에서
  원작 1.8 코드가 즉사하던 지뢰밭 정리 — 몬스터볼 제보의 유력 원인(파티 만석 +
  `nitems`)도 여기 덮인다. 도구 `share/patch_ruby_compat.py`·감사
  `share/qa-ruby-compat.py`.
- 도구: 딱지 붙은 dat 판독을 `vendor/datread.py` 한 자리로(멎어 있던 build·probe·verify
  복구) · dat 손수정 회수 `harvest.py` 신설 · 조사 판정의 낫표 건너뛰기 루비 1.9+ 결함
  수리(현행 배포 문장에는 걸리는 자리 없음 — 다음 코어를 만들 때 실린다).
- 문서: 네 층 재편(게이트 AGENTS · 지침 `docs/guides/` · 대장 `docs/ledger/` · 기록
  `docs/log/`) — events-and-speech 신설, mods를 packaging·release로 분할.
  이어서 대장을 근거 전용으로 좁혔다(08-09): 현행 규칙은 지침으로, 열린 일감은 로드맵과
  티켓 파일로 갈라 대장이 24k에서 8k 토큰이 됐다. 고아 폴더 `docs/design`을 없애고
  깨진 링크 열여섯을 고쳤다.

## v5.2.1 (2026-08-07, 자산 교체 2026-08-07)

세 합본 zip을 같은 판 번호로 갈아 끼웠다(받은 사람이 몇 안 되어 유지자가 교체를 택했다).
옛 zip과의 차이는 셋뿐 — `mkxp.json`이 새로 들어가고, `mod.json`의 자산 목록이 한 줄 늘고,
`읽어주세요.txt`의 설치 절이 바뀌었다. 나머지 241개 파일은 바이트까지 같다.
릴리스 본문에는 「지난 판에서 덮어쓰시는 분께」·「아이폰·안드로이드에 넣으실 때」 두 절을 더했다.

- 합본에 원본 배포판의 `mkxp.json`을 동봉한다. v5.1·v5.2가 넣었던 `fontSub`(글꼴 이름 14종을
  `Galmuri11`로 꺾는 표)가 덮어쓰기로 살아남으면 v5.2.1의 글꼴이 옛 Galmuri11로 되돌아가기
  때문이다. 원본과 우리 옛 판의 차이는 그 표와 기본값과 같은 `smoothScaling` 한 줄뿐이라
  잃는 설정이 없다(실측 — 원본 sha256 `569938…`, 지금 설치본과 바이트 일치).
  ⚠ **옛 판에서 올라올 사람이 한 바퀴 지나면 다음 판에서 뺀다.**
- 안내문의 설치 절을 고쳤다. 「폴더를 통째로 덮어쓰라」가 모바일 파일 관리자에서 **폴더 교체**로
  읽혀 `Data`의 원판 맵이 통째로 사라진 제보가 있었다 — 폴더 안의 파일을 덮으라는 문구로 바꾸고,
  세이브가 게임 폴더 밖(`Saved Games\Pokemon Z`)에 있어 판을 갈아도 이어진다는 것을 적었다.
- 유지자 눈 대조로 글꼴 셋을 확인했다(Z-8 닫음) — 글자 크기가 고르고 대사가 넘치지 않는다.

## v5.2.1 첫 배포 (2026-08-07)

글꼴을 갈아 끼우고 배포 구성을 합본 셋으로 바꾼 판. 본문:
[릴리스 노트](log/research/2026-08-07-release-v5.2.1-notes.md)

**번역**
- 리본 이름 46자리를 본가 정식명으로. 코퍼스에 있던 16자리(뚝심·철렁·풀죽기·덜렁이·상쾌·
  잠보·방글방글·고저스·브로마이드·레코드·레전드·프리미어·메모리얼·위시·에리어챔피언)와
  PKHeX 리본표로 확정한 콘테스트 리본 30자리(근사함→쿨 · 아름다움→뷰티 · 귀여움→큐트 ·
  슬기롭음→지니어스 · 강인함→파워풀). 영문이 반쯤 남아 있던 여덟 자리도 함께.
- 한자·일본어 잔재 5자리 — 「눈에映진」·「장수(長壽)」·「쫒아다닌다」·「고ージャ스리본」 등.
- 통통코 도감을 본가 자구로. 깨진 도감 세 줄에서 위키 링크 문법과 중복절 제거.
- 특성 변경 화면 문구 셋 — 번역표에 키 자체가 없어 스페인어가 나오던 자리.
- 화면 부호를 활자 부호로 — 큰따옴표 203 · 작은따옴표 117 · 어퍼스트로피 389 · 대괄호 39 ·
  화살표 4. 픽셀 폰트에 ASCII 따옴표·대괄호가 없어서이기도 하고, 한국어 표기로도 이쪽이 맞다.

**화면**
- 게임이 요청하는 폰트 패밀리 16종을 한글 든 픽셀 폰트가 직접 들도록 개명해 넣었다. 폰트 대체
  설정(`fontSub`)에 기대지 않으므로 조이플레이처럼 그 설정을 안 읽는 엔진에서도 잡힌다.
- 폰트에 없던 106자를 갈무리에서 들였다 — 대괄호·따옴표·낱자모(ㄱ~ㅣ)·문장부호, 그리고 찌그러지던
  도형 21자. 한글 음절은 손대지 않았다(2,910 → 2,995자, 375KB).
- 글자 그림자를 1.25픽셀로 — 1픽셀 세 겹에 2픽셀 대각을 옅게(알파 48) 얹는다.
- 한글 굵게를 엔진과 같은 방식으로 다시 만들었다. 엔진은 글리프 폭을 글자 크기의 10분의
  1만큼 넓히는데(SDL_ttf `glyph_overhang`), 그 계산을 그대로 쓰되 한 눈금 덜어낸다 —
  대사창 크기에서 1픽셀, 32픽셀 글자에서 2픽셀. 덜어낸 자리에 옅은 한 겹(알파 140)을 얹어
  그 사이 반 눈금을 만든다. 크기에 비례하므로 큰 글자에서도 굵기가 따라 올라간다.
- 두 모드로 갈랐다 — 글꼴 모드와 `한글패치 통합-Runa`(번역표·코어·자산). 번역 모드가
  `requires: ["hangul-font"]`로 글꼴 모드를 능력으로 가리킨다.
- 글꼴이 셋이 됐다 — `DPPT Font`·`Galmuri Font`·`BW Font`. DPPt와 BW는 한글 음절
  2,355자만 다른 형제고, 갈무리는 원본 통짜를 우리가 쓰는 2,996자로 줄인 것이다
  (3.5MB → 389KB). 셋은 서로를 `conflicts`로 밀어낸다.
- **한글이 크기마다 다른 높이로 그려지던 것을 고쳤다.** FreeType의 자동 힌팅은 한자 몇
  자의 윤곽선을 재서 CJK 기준선을 잡는데, 우리 글꼴에는 한자가 없었다(갈무리는 줄이며
  버렸고, DPPt·BW는 애초에 없다). 그래서 같은 높이여야 할 한글이 25·26·28·31픽셀에서
  두세 무리로 갈렸다 — 게임이 쓰는 크기가 16의 배수가 하나도 없어서다. 갈무리는 한자
  39자를 남기고, DPPt·BW는 한글과 같은 높이의 속 빈 네모를 그 자리에 심어 기준선을
  만들었다. 일곱 크기 전부에서 두 무리(받침 있음·없음)로만 떨어진다.
  이분 탐색으로 「他」 한 글자면 충분한 것도 확인했다.

**도구**
- 문장 코퍼스에 영어 칸 추가(9개 게임 163,106쌍). 게임 스크립트가 영어 리터럴을 쓰는 자리를
  본가와 대조할 다리가 생겼다.
- `canon.jsonl`에 리본 도메인(PKHeX 리본표 164종). 4,824 → 4,988행.
- `verify.py`에 리본 대조(`check_ribbons`) — 등급 붙은 이름까지 본다.
- `build.py`에 `--out=` — 보관소·게임을 건드리지 않고 시험판만 뽑는다.
- modkit 카드에 `provides` — 모드가 「한글 폰트 제공」 같은 능력을 선언하고, 다른 모드가
  특정 이름 대신 그 능력을 요구할 수 있다(modkit `d5c8c11`). 한글패치를 번역과 폰트로
  가르는 데 필요했다.

**최신 루비 실행기 갈래** — `runa-utf8`을 main에 병합
- `korean.dat` 문자열에 UTF-8 인코딩 딱지. 딱지가 없으면 루비 1.9+ 실행기에서
  `Encoding::CompatibilityError`로 멎고, 비ASCII 표제의 조회가 빗나간다
  (절23 표제 6,819개 중 2,451개 — 딱지를 붙이면 0개, 루비 3.1.7 실측).
- 루나판 모드를 재료에서 짓는 조립기 `runa/make-patch-mod.py`·`runa/stamp-fonts.py`. 손으로 만든 유일본이던 폴더를
  번역표 정본과 「한글패치 통합」에서 매번 다시 짓는다 — 같은 재료면 바이트까지 같고,
  정본 게임 사본에 얹었다가 빼 보니 파일 19,070개가 원래대로 돌아왔다.
- 「한글패치 통합」 코어에 박혀 있던 주입 섹션 13개 제거. 루나판 폴더와 하드링크로
  이어져 있어, 루나판에 코어를 복사한 동작이 원판까지 함께 바꿔 놓았던 자리다.
- 글자가 그려 박힌 그림 세 장(배틀 커맨드 버튼·타입 아이콘 둘)을 루나판에서 뺐다 — 2026-08-04에
  GUI 모드 몫으로 넘긴 자리인데 파일만 남아 계속 딸려 나가고 있었다.

**배포**
- 합본 셋(DPPt·갈무리·BW)과 디버그 코어 단품으로 갈랐다. 셋은 Fonts의 16벌과 글꼴
  라이선스 원문 말고는 바이트까지 같다. 디버그 코어는 글꼴과 무관해 어느 합본에든 통한다.
- 조립기가 스테이징에 남은 modkit 작업 파일을 걷어 낸다. `Data/Scripts.rxdata.orig`가
  **v5.2 배포물 셋에 실려 나갔었다** — 설치하면 진짜 순정 백업을 밀어내고 그 자리를
  차지해, 그다음 제거가 순정 대신 번역판 코어를 되살린다.
- 주입 섹션의 `MOD:` 표를 떼어 합본 자체 섹션으로 굳힌다. 표를 단 채로 두면 modkit
  설치가 화면 한글화와 글꼴 스크립트를 통째로 걷어 간다.

## v5.2 (2026-08-06)

주연 4,150행과 배틀 대사 532행 재번역, 선별 623행 판정·반영, 상태이상 이름을 「부패」에서
「쇠락」으로, zip에 `mod.json` 동봉해 모드로 설치 가능. 자산 넷(통합 · 순수 번역 ·
스크립트 모드 묶음 · 디버그판).
본문: [릴리스 노트](log/research/2026-08-06-release-v5.2-notes.md) ·
[v5.1 이후 전수 조사](log/research/2026-08-06-v5.1-to-v5.2-changes.md)

## v5.1 이전

v5 계열 패키징 체계(3종 변형)와 그 이전 이력은
[번역 품질 작업 경위](log/2026-08-08-quality-plan-and-progress.md)의 절별 기록을 본다.
