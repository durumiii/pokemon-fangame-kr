# Pokemon Z 번역 품질 개선 — 조사와 플랜

> 2026-08-01. 사용자 방향: ① 쉬운 것부터 — 인명 + 공식 용어 통일(본가 번역 DB 참고,
> 없으면 조사·창작), ② 인물별 말투 설정과 직역 냄새 제거는 해볼 만하되 **토큰이 많이
> 드는 만큼 품질과 비용을 미리 조사**할 것. 이 문서가 그 조사와 실행 계획이다.

## 재료 확보 상태 (전부 실측)

- **판독·되쓰기가 뚫렸다.** `korean.dat`은 Essentials 다국어 포맷(절 24개,
  `MessageTypes` 상수와 1:1 — 1=종, 5=기술, 7=도구, 10=특성, 14=NPC명, 19=지명,
  21=맵이름, 23=스크립트 문장). 대사 쌍은 OrderedHash의 중첩 Marshal(`[keys, values]`)에
  있고, `fanlib.rubywrite`로 **왕복 구조 일치 + 값 교체 후 재판독까지 검증됐다.**
- **코퍼스 추출 완료**: 스크래치패드 `korean_corpus.json`. 스페인어 원문↔한국어 쌍
  **20,597건**(맵 대사 13,770 + 스크립트 문장 6,827) + 이름 목록형 8,512건.
  ES 1,241,310자 · KO 746,940자, 번역문 길이 중앙값 42자.
- **산출 경로**: 갱신한 `korean.dat`을 「한글패치 통합」 모드(보관소)에 넣고 라이브러리로
  재설치. 원본은 모드의 정본 사본이 보존한다.

## 번역 정본 — korean.dat는 이제 빌드 산출물이다 (2026-08-02)

**정본은 repo의 `mod/z/translate/ko/`다** — 절별 JSONL, 한 줄이 한 문장(원문+한국어),
줄 순서가 dat의 자리 순서. `build.py`가 정본에서 korean.dat를 구워 왕복 검증 후
보관소·게임에 앉힌다. **앞으로 번역 수정은 ko/ 파일에만 하고 build로 굽는다** —
dat를 직접 문지르는 스크립트를 부득이 쓰면 직후에 `export.py`로 정본을 재동기화한다.
게임 업데이트로 원문이 바뀌면 build가 골격 불일치로 멈추고, 그때도 export부터.
이 구조 덕에 수정·검수·LLM 재번역이 전부 git 줄 단위 diff로 관리된다.

## 갈래 1 — 인명 + 공식 용어 (싸고 체감 큰 쪽)

### 인명 (가장 큰 구멍)

- NPC 이름 절(14) **362건 전원 로마자**. 상위 20명이 대사 속에 **3,760회** 등장
  (Crisanto 771 · Melia 543 · Olivier 418 · Mirra 323 · Alca 290 …).
  대사 20,597줄 중 로마자 낱말 포함이 6,814줄 — 대부분 이 인명들이다.
- 방법: ① LLM 한 번으로 362명 음차안 생성(스페인어/프랑스어 발음 기준 병기) →
  ② 사용자 검수(상위 빈도부터) → ③ 기계 적용 — 절14 교체 + 대사 내 문자열 치환.
  치환은 `<b>이름</b>`·`이름:` 패턴이 대부분이라 안전하고, 짧은 이름은 단어 경계를
  검사한다. 비용은 사실상 0(호출 한두 번).
- 지명도 같은 틀: 절19는 이미 번역됨(1번도로), 절20에 혼혈("Merlot 저택") 잔존 — 함께 검수.

### 공식 용어

- **일반 용어는 생각보다 건강하다**(실측): Centro Pokémon→포켓몬센터 52/54,
  entrenador→트레이너 지배적, Poké Ball→몬스터볼. 갈라진 것은 `combate`
  (배틀 159 · 시합 7 · 대결 6)과 따옴표 스타일 정도 — 규칙 치환으로 통일 가능.
- **게임 데이터 명칭은 본가 DB(우리 Wishing Star 판독 `packed.json`, 검산된 한국어명)와
  전수 대조했다**: 종 18건 · 기술 41건 · 특성 32건 불일치. 내용은 세 부류가 섞여 있다 —
  옛 공식명(→현행 공식명으로 갱신), Z 창작 콘텐츠(→포켓몬 번역 스타일로 창작), 표기
  변형(공백·구판 표기). 실행 시 한 건씩 분류한다. **도구 905건 전부 불일치는 대조
  코드가 packed의 도구 행 구조를 잘못 짚었을 가능성이 커서 무효 — 다시 잰다.**
- 이 명칭들은 대사 본문에도 등장하므로, 명칭 확정 후 대사 쪽도 같은 치환을 태운다.

## 갈래 2 — 인물별 말투 + 직역 제거 (LLM 배치)

### 재료

- **화자 태그가 이미 붙어 있다**: `<b>이름:</b>` 프리픽스가 3,398줄, 화자 199명.
  상위 15명이 태그 대사의 약 72%를 덮는다 — **말투표는 상위 ~20명 + 기본값 셋**
  (일반 NPC 반말/해요체, 시스템 문구 평서)이면 충분하다.
- 무태그 대사는 맵·인접 대사 문맥으로 화자를 추정하거나 기본값을 쓴다.
- **보존해야 하는 마크업**(기계 검증 가능): `<b>` 6,406 · `\c[n]` 5,336 · `<i>` 579 ·
  `\PN` 486 · `\se`·`\sh`·`\wtnp[n]` 등. 재작성 후 원문과 마크업 종류·수 일치를
  기계로 검사하고, 불일치는 자동 반려·재시도한다.
- 길이 제약: 원문 대비 과도한 팽창(>1.4배)을 반려 기준에 넣는다(대사창 폭).

### 파이프라인 설계 (개요)

1. 맵 단위로 대사를 묶어(문맥 유지) 배치 요청 생성 — 쌍 30~60개/요청, 프롬프트에
   용어집(갈래 1 산출) + 화자 말투표 + 규칙 첨부.
2. 구조화 출력(스키마 강제)으로 `[{id, ko}]`를 받는다.
3. 기계 검증(마크업·길이·플레이스홀더) → 불합격만 재시도.
4. 표본 감사: 무작위 표본을 원문 대조(뜻 보존) — humanize-korean의
   content-fidelity 개념을 축소 적용.
5. `korean.dat` 재조립 → 모드 갱신 → 실기 확인.

### 비용 (2026-08-02 확정 — 아래 옛 산정은 폐기)

경로는 **llmgateway + gemini-3.6-flash + reasoning_effort=minimal**로 확정됐고,
실측 비용은 **전량 초벌 ~$8(상한 $10)**이다. 산정 근거·설정·범위는 실행 승인 문서
[`z-batch-run-proposal.md`](z-batch-run-proposal.md)가 정본이다. 애초 이 자리에 있던
Anthropic Batch API 기준 표($4~19)는 그 결정 이전의 산정이라 지웠다 — 씽킹 과금
발견과 effort 실측이 그 표를 무효로 만들었다(같은 문서 4절).

### 품질×비용 사전 검증 — 파일럿 (플랜의 첫 실행 단계)

전량을 돌리기 전에 **표본 200쌍**(화자·문체 다양하게)을 모델 2~3개(예: Sonnet 5 배치 /
Haiku 4.5 / 저가 게이트웨이 모델)로 돌려 나란히 놓고 사용자가 눈으로 고른다.
파일럿 비용은 모델당 $0.1 미만. 여기서 모델·프롬프트·반려 기준을 굳힌 뒤 전량 배치.

## 실행 순서 (2026-08-01 저녁 갱신 — 수집·검수 완료, 적용 단계 진입 대기)

**끝난 것**: 인명 362건 판별·음차·검증(공식 6건 확정, 4~7묶음 데이터 판정 —
[`../research/2026-08-01-z-names-proposal.md`](../research/2026-08-01-z-names-proposal.md)),
배틀 표현집 195행([`../research/2026-08-01-z-battle-expressions.md`](../research/2026-08-01-z-battle-expressions.md)),
공식 인명 ES↔KO 485쌍 CSV(`mod/z/tools/bulba/`), 결정 확정(조사 플러그인 포함·데미지·
현행 문구·어감 우선).

**다음 걸음** (번호는 착수 순):

1. **인명 적용 — 됐다(2026-08-01, 실기 확인 대기).** 정본 명단은
   `mod/z/translate/names.json`(362건 = 교체 354 + 원문 유지 8), 스크립트는
   `apply_names.py`. 절14 전량 + 절13 칭호 네 자리(58·103·148·192, 전용 클래스
   실측 확인) + 한글 대사 3,213개 값에서 3,587회 치환 + 자나→사나 3회. 왕복 검증
   통과, 잔존 라틴 이름 0건. 보관소·게임 양쪽 기록, 원본은 `korean.dat.orig`.
2. **조사 선택 플러그인 — 됐다(2026-08-01, 실기 확인 대기).** `mod/z/mods/Josa Select`
   — `\j[받침형,무받침형]` 문법, (으)로 ㄹ 특례·숫자·알파벳 읽기 판정, 판정 불가면
   무받침형. 훅 넷: 창 둘(Advanced·Unformatted text=)과 비트맵 직그리기 둘
   (drawTextEx·drawFormattedTextEx). 판정 로직은 파이썬 포트 12케이스로 검증.
   기존 병기 1,192건(절22 16 + 절23 1,176)을 `apply_josa.py`가 `\j`로 변환해
   양쪽 기록 완료. **루비 1.8 실행 검증은 실기 부팅이 첫 관문이다**(로컬에 루비 없음).
3. **용어 적용 — 됐다(2026-08-01).** 전수 재대조 결과 Z 패치는 이미 현행 공식명과
   거의 전부 일치 — 수정은 기술 셋(깨트리기·탐내기·프레젠트)뿐이고 종 18·기술 41·
   특성 32·도구 905라는 옛 수는 기준 쪽 구명/조인 오류/영어 기준 오독이었다.
   combate 13건은 배틀로 통일. 근거와 기준 DB의 세대 함정(PokéAPI=2020 대개명 이전)은
   [`../research/2026-08-01-z-terminology-audit.md`](../research/2026-08-01-z-terminology-audit.md).
4. **배틀 표현 적용 — 됐다(2026-08-01, 실기 확인 대기).** 절23에서 표현집과 다른
   94자리를 전통 표현으로 교체(상태이상·날씨·설치기·급소/상성/실패·랭크·포획·경험치 —
   "{1}의 {2}!" 기술 사용 문구 포함). 교체 목록은 `mod/z/translate/battle-expr-replacements.json`
   (원문·구·신·근거 행), 적용은 `apply_battle_expr.py`(old 일치·플레이스홀더 검증).
   같은 날 병기 반대 꼴 191건도 `\j`로 마저 변환해 병기 잔존 0.
5. **파일럿 — 됐다(2026-08-02).** 표본 200쌍 × 7모델(클로드 3 + 게이트웨이 4) →
   기계 검증 → 사용자 1차 판정(Opus·Gemini 3.5/3.6만 생존) → Fable 판정관의
   논쟁 30행 판정. 결론: 초벌은 gemini-3.6-flash — 실패 결이 기계 검증으로 잡히는
   부류라서다. 근거: [`../research/2026-08-02-z-pilot-comparison.md`](../research/2026-08-02-z-pilot-comparison.md) ·
   [`-adjudication.md`](../research/2026-08-02-z-pilot-adjudication.md).
6. **준비물(0단) — 됐다(2026-08-02).** 말투표 42인(`mod/z/translate/voices.md`,
   별칭 병합 `speaker-aliases.json`) · 용어집(`glossary.md`, 고정 용어표 17 —
   포켓프랑·입법관·선장·그림자 포켓몬·후보생 판정 포함, 기존 표기 235행 스윕) ·
   프롬프트 v1(`prompt.md`) · 검증기 7종(`validate.py`, 파일럿 산출로 보정 —
   Fable 판정관이 잡은 #72를 스스로 잡음).
7. **초벌 배치 — 됐다(2026-08-02).** 실행 계획서
   [`z-batch-run-proposal.md`](z-batch-run-proposal.md) 승인 후 발사.
   14,242행·597청크·gemini-3.6-flash(reasoning minimal — 씽킹은 존재 강제이나
   양은 조절됨, 실측 A/B로 품질 동급·비용 3.2배 확인). 러너는 `batch.py`
   (재개·ETA·redo·apply). 결과: 통과 14,206(변경 12,265 반영) · 반려 재시도 후
   잔여 36(0.25%, 현행 유지) · 실비용 $9.04. apply 뒤 스윕 셋 — 라틴 화자 태그
   복원 697행(names.json 치환) · Rúpico 악센트 변형 24 · 통째 영어/스페인어 행
   19 복원(게이트가 언어를 안 봐서 통과했던 것). 조사 배경: 말뭉치
   전수(`../research/2026-08-02-z-corpus-survey.md`) ·
   적용 구조(`../research/2026-08-02-z-intl-architecture.md` — stringToKey 정규화 함정으로
   도달 불가 키 73개 발굴·수선) · 종합 보고서(`z-retranslation-methodology.md`).
8. **발굴(3단) — 됐다(2026-08-02).** 재현율 측정(`pilot/recall_probe.py`,
   판정 문서의 결격 실례 10 + 깨끗한 행 20)으로 구성 확정: **점검표 프롬프트**가
   열린 질문 대비 luna를 4/10→7/10으로 올렸고(프롬프트가 모델보다 컸다),
   luna×2 합집합 8/10 — 늘 놓친 둘은 「경미」 부류. deepseek v4·kimi-k3·qwen3.7은
   2~3/10 탈락. 본편은 luna×2 + glm-5.2×1(사용자 선택), 14,134행×3통과 $7.80.
   합집합 3,724행 → 계열 필터(용어집 재지적·띄어쓰기 관례·프랑스어 유지 지시
   재지적 소거, 미번역 잔재 49행 별도) 후 3,569행. 러너 `mine.py`.
9. **좁혀진 검토(4단) — 됐다(2026-08-02).** llmgateway 프리미엄 소진으로
   **구독 CLI**(`claude -p --model opus`, `--bare`는 로그인 미탑재로 불가)로 판정.
   전 후보 3,569행 판정: fix 857 · minor 1,744 · ok 961(발굴 오탐 기각 — 「노나」를
   오타로 몬 3표 지적을 근거 달아 뒤집음). 1표 표본 50의 실결함률 6% 외삽(~96)과
   달리 전체 판정은 214 — **작은 표본 함정 재발**, 1표도 전량 판정이 맞았다.
   fix 855행 정본 반영(게이트 반려 0), 러너 `judge.py`.
10. **재조립(5단) — dat 구움(2026-08-02, 실기 확인 대기).** `build.py` 왕복 검증
    통과, 값 차이 12,431곳 반영, 보관소·게임 양쪽 기록. 걸음 5 파이프라인 총비용
    **~$17**(초벌 $9.04 + 발굴 $7.80, 판정은 구독 쿼터 — 현금 0이지 공짜 아님).
11. **소배치 둘(fill) — 됐다(2026-08-02).** ① 절23 미번역 새 번역: 3,174행을
    게임 스크립트 대조로 갈라(본편 994 + 판정 불가 302만, 에디터 463·죽은 1,415
    제외 — 판별이 `fill.py plan`에 코드로 남았다) 새 번역 프롬프트로.
    ② 설명문 절(3·6·9·11·20) 2,883행 도감체 다듬기 — 2,482행은 모델도 현행 유지.
    108청크 반려 0, $1.73, 1,430행 반영·재조립.
12. **잔재 일괄 + Caduco 판정 — 됐다(2026-08-02).** 발굴이 모은 미번역 잔재 중
    미해결 27행을 구독 Opus 한 호출로 번역(트레이너 팁·포고꾼·그린이 @핸들 보존).
    그 과정에서 **Caduco가 이 게임의 여섯째 상태이상**(PBStatuses::CADUCO=6,
    반피 이하 피해 1.5배)인데 말뭉치에서 아홉 갈래로 옮겨져 있던 것을 발견 —
    **상태는 「부패」**(연금술 putrefactio, 팀 아조스 테마 정합), 지명은 「카두코
    구호소」로 판정·전수 통일, 용어집 고정. 남은 것: **실기 표본 확인** ·
    0표 잔존(발굴 재현 8/10의 그늘 — 경미 치우침 추정, 재훑기 1통과 $2.6에 열려 있음).

**4.5 — 기초 표현·미번역 청소 (2026-08-02, 사용자 피드백으로 추가·완료)**:
① 배틀 외 기초 표현 50자리(기술 잊기 「1, 2, ... 짠!」·진화 「{1}의 모습이...!」·
리포트·닉네임·상점·PC·낚시·부화) — 표현집 10절로 증보. ② 시스템 어휘 49건
(성격 25·능력치 7·가방 주머니 7·상태 6·라벨 4) — 영어 키가 미번역으로 화면에
새던 자리. ③ 상대/야생/아군 접두 템플릿 — 「슬리프 rival」의 원인. ④ 스크립트
하드코딩 문자열은 dat로 못 고쳐서 **UI Text KR 주입 모드**(교체표 + 그리기 훅)로:
일시정지 단축키·출현 안내판·배지 12종. 새 하드코딩 발견 시 그 표에 한 줄 추가.
**남은 미번역**: 절23에 원문=값이 약 3,000건 있으나 대부분 개발용 문자열이고,
보이는 부류(퀘스트명·긴 안내문 등)는 갈래 2 배치에서 함께 처리한다.

각 단계 산출은 별도 커밋, `korean.dat` 갱신마다 왕복 검증 필수. 산출을 게임에 앉힐 때는
「한글패치 통합」 모드 갱신 → 라이브러리 재설치 흐름.

13. **용어 판정 연쇄 — 됐다(2026-08-02).** Hemorragia=출혈(창작 상태이상 7번, 급소 +2),
    Medalla=배지(본가 존중 — 대사 93행 + UI 모드 12종 「음차+배지」 통일),
    versión exótica=다른 지방의 모습(리전폼은 비공식 약칭 — 공식 어법 「(지방명)의 모습」),
    랭크 변화 문구 공식 체계(1랭크 올라갔다/떨어졌다 · 2랭크 크게 · 3랭크+ 매우 크게 —
    상승 계열 6행이 「올랐다」로 어긋나 있던 것 교정, 사용자 전사 근거). 전부 용어집 고정.
14. **공유 패키지 — 조립 체계 완성(2026-08-02, 배포일 미정).** `mod/z/share/make_package.py`
    한 방이면 배포판이 선다 — 원 패치 에셋 182 + 최신 dat + Josa Select·UI Text KR 주입
    Scripts.rxdata + 번역표(정본 24파일) + 독립 빌더(rubywrite 동봉, pip rubymarshal만 요구)
    + 번역 도구(prompt·glossary·voices·aliases — 남이 장면 재번역할 때의 일관성 재료).
    읽어주세요는 자체 양식으로 새로 썼다 — 번역 방식 5단 서술, 본가 이름 정책(논쟁적 선택임을
    명시), 창작 고유명사 용어표. 불러오기 화면 지명이 번역표를 안 지나던 경로도 고쳤다
    (pbGetBasicMapNameFromId 덮어쓰기 — UI Text KR).
15. **3언어 공식 명칭 사전 — 됐다(2026-08-02).** `translate/reference/trilingual-terms.jsonl`
    311항목 전부 verified. 위키 수집 → PokeAPI CSV 기계 재검증(진짜 오류 0 — 어긋남은 전부
    스페인/중남미 지역 분화, 알그룹 3건은 PokeAPI 쪽이 틀림) → pret 팬번역 소스·사용자 전사로
    잔여 승격. 동명 함정 실사례 셋(발레리→마슈, 초련→고지카, 왈라스→윤진 — 셋 다
    비슷한 이름의 딴 인물 문서를 인용했던 것)이 요약 문서에 남아 있다.
16. **도감 정식판 스왑 — 됐다(2026-08-02).** 도감 설명·분류가 본가 복사임을 실측(설명
    1,018 중 748이 영어/스페인어 원문 그대로)하고 PokeAPI CSV 매칭(`dexswap.py`)으로 교체:
    분류 423(공식 — 하느라기 프릴 등), 설명은 같은 판본 공식만 — exact 315 + 다이아몬드 41
    (BDSP 덤프→podic DP 원문 정렬) + 화이트 50(podic). **다른 판본 공식으로 갈아치우는
    fallback은 사용자 판정으로 기각** — 번역 프로젝트에 제작자의 판본 선택을 바꿀 권한 없음.
    에메랄드 337은 한국어 공식이 존재하지 않아(3세대 미발매) 나무위키 2021 덤프의 팬번역을
    사용자 판정으로 채용(`namudex.jsonl` ETL, 캐이시 검수 통과). **전사원 서열 실측**:
    podic.kr이 컴파일 공식 덤프와 41종 중 31종 글자 일치(잔여는 DP↔BDSP 공식 개정 차) —
    공식 전사는 podic, 미발매 팬번역은 나무위키 덤프, 리브레·나무 위키는 의역 오염 있음.
    미파싱 잔여 4행(#59·#140·#366·#386)은 우리 번역 유지.

## 실기 관찰 첫 소견 (2026-08-02 사용자 — 다음 세션의 표적)

관찰은 장기이고 이건 첫 사흘 치 소견이다. 셋 다 성격이 다르다:

1. **NPC 대사가 재번역 전처럼 읽힌다 — 원인은 딱딱한 「~다.」 번역투.** 포켓몬 세계의
   NPC는 그 어투를 안 쓴다. 말투표가 주연 42인만 덮고 일회용 화자는 「일반 NPC 해요체」
   기본값 하나뿐이라, **일회용 화자에게도 몇 가지 어투를 배정하는 조사·설계가 필요하다**
   (다음 세션 1순위 — 조사부터).
2. **배틀 문구 어색 잔존**: 씨뿌리기 「~가 감염됐다!」, 앙코르 「~는 저주받았다!」 류.
   걸음 3~4 표현집이 못 덮은 자리 — 본가 정식 문구로 표현집을 넓혀 교체(사전·podic이
   재료다).
3. **주연 어투 오적용이 드물게 남는다**: 멜리아가 올리비에에게 반말(청자를 주인공으로
   착각한 듯). 청자 맥락을 완벽히 짚는 비싼 모델이나 손번역 없이는 어려운 부류 —
   **어쩔 수 없음으로 접수**, 발견 시 개별 수정.

## 실기 소견 둘째 묶음 (2026-08-02 밤 — 같은 날 처리 완료)

1. **아이템 획득 문구 스페인어 — 삼세번 만에 진범 확정: stringToKey 오독.**
   1차 「런타임 키 불일치 추정」·2차 「오전 수선으로 이미 살아남(재시작 대기)」은
   둘 다 틀렸다 — 파이썬 시뮬레이션이 게임 코드와 다르게 굴렀기 때문이다.
   **포터블 루비로 게임의 OrderedHash·stringToKey를 그대로 돌려 실물 dat 조회를
   재현하니 MISS** — 루비 `^`/`$`는 줄 앵커라 `\r\n`이 공백이 아니라 **`\n`**으로
   정규화된다(적용 구조 문서 §4-4 정정 참조). 오전의 73키 수선은 깨진 CRLF형을
   또 다른 깨진 공백형으로 바꿔 놓은 것이었다. 수선(같은 날 밤): build.py
   정규화를 루비 충실형으로 교정(오라클 대조 20,715키 전량 일치), dat 키 수술
   (참모양 rename 28 + 중복 병합 63, 소실 번역 4건 이식) 후 export 재동기화.
   **최종 검증은 루비 실물 조회**: 줍기(포켓5)·상대 내보내기·도둑·기술 잊기·
   드랍 다섯 문구 전부 HIT. _INTL 전수 감사 잔여는 화면 도달 1건(점 연출)뿐.
   교훈: **다른 런타임의 함수는 그 런타임으로 검증하라** — 파이썬 재현 두 번이
   연속으로 같은 자리에서 틀렸다.
2. **capitán 재판정 — 완료.** 맥락 전수(179행)로 이원화: **군 계열은 「대장」**
   (메를로=국왕 근위대·총사 지휘, 크리산토=미라 정부군, 센데라=프락탈시티 영주 —
   선장/대장/대위 3갈래 혼재 136행을 대장으로 통일, 절13 클래스 2자리 포함.
   트레이너 데이터 실측: 클래스 36=MERLOT·107=CRISANTO5), **배·해적 결은 「선장」
   유지**(로토와 선원단 21행). glossary·voices 반영.

## 티켓

- **특성 발동 플레이버 문구 전수 대조** (2026-08-02 등록): 원장(표현집)에 없는
  특성 발동·플레이버 자리는 초벌 품질에 의존 중. 본가 텍스트 덤프류 사전(pkhex 계열
  등)에서 전거가 무조건 나오는 부류라 마음먹으면 기계 대조 가능. 착수 전까지는
  실기 제보 단위 개별 수선.

## 미결 (사용자 결정)

- 인명 음차 기준: 스페인어 발음(크리산토) vs 프랑스어풍 이름은 프랑스어 발음 —
  음차안에 병기해서 검수 때 고르는 방식 제안.
- 말투표: 상위 화자 20명의 말투를 어떻게 정할지 — 파일럿 표본에 화자별 대사 묶음을
  같이 뽑아 성격을 보고 정하는 방식 제안(게임을 플레이한 사용자의 감이 정본).
- 전량 배치 모델: 파일럿 결과로 결정.

## 확정된 결정 (2026-08-01 사용자)

- **조사 선택을 번역 모드에 포함한다** — `\j[은,는]` 류 조사 자동 선택 플러그인을 주입
  모드로 함께 넣고, 번역문은 그 문법으로 쓴다(「○○은(는)」 병기 회피).
- **표기는 「데미지」**, **문구는 현행(최신 세대) 기준** — 3세대와 갈리는 자리는 현행 쪽.
- 인명 음차는 **한국어 어감 우선**(발음 원칙과 갈릴 때).
